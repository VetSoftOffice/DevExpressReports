<?xml version="1.0" encoding="utf-8"?>
<root>
  <!-- 
    Microsoft ResX Schema 
    
    Version 2.0
    
    The primary goals of this format is to allow a simple XML format 
    that is mostly human readable. The generation and parsing of the 
    various data types are done through the TypeConverter classes 
    associated with the data types.
    
    Example:
    
    ... ado.net/XML headers & schema ...
    <resheader name="resmimetype">text/microsoft-resx</resheader>
    <resheader name="version">2.0</resheader>
    <resheader name="reader">System.Resources.ResXResourceReader, System.Windows.Forms, ...</resheader>
    <resheader name="writer">System.Resources.ResXResourceWriter, System.Windows.Forms, ...</resheader>
    <data name="Name1"><value>this is my long string</value><comment>this is a comment</comment></data>
    <data name="Color1" type="System.Drawing.Color, System.Drawing">Blue</data>
    <data name="Bitmap1" mimetype="application/x-microsoft.net.object.binary.base64">
        <value>[base64 mime encoded serialized .NET Framework object]</value>
    </data>
    <data name="Icon1" type="System.Drawing.Icon, System.Drawing" mimetype="application/x-microsoft.net.object.bytearray.base64">
        <value>[base64 mime encoded string representing a byte array form of the .NET Framework object]</value>
        <comment>This is a comment</comment>
    </data>
                
    There are any number of "resheader" rows that contain simple 
    name/value pairs.
    
    Each data row contains a name, and value. The row also contains a 
    type or mimetype. Type corresponds to a .NET class that support 
    text/value conversion through the TypeConverter architecture. 
    Classes that don't support this are serialized and stored with the 
    mimetype set.
    
    The mimetype is used for serialized objects, and tells the 
    ResXResourceReader how to depersist the object. This is currently not 
    extensible. For a given mimetype the value must be set accordingly:
    
    Note - application/x-microsoft.net.object.binary.base64 is the format 
    that the ResXResourceWriter will generate, however the reader can 
    read any of the formats listed below.
    
    mimetype: application/x-microsoft.net.object.binary.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Binary.BinaryFormatter
            : and then encoded with base64 encoding.
    
    mimetype: application/x-microsoft.net.object.soap.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Soap.SoapFormatter
            : and then encoded with base64 encoding.

    mimetype: application/x-microsoft.net.object.bytearray.base64
    value   : The object must be serialized into a byte array 
            : using a System.ComponentModel.TypeConverter
            : and then encoded with base64 encoding.
    -->
  <xsd:schema id="root" xmlns="" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata">
    <xsd:import namespace="http://www.w3.org/XML/1998/namespace" />
    <xsd:element name="root" msdata:IsDataSet="true">
      <xsd:complexType>
        <xsd:choice maxOccurs="unbounded">
          <xsd:element name="metadata">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" />
              </xsd:sequence>
              <xsd:attribute name="name" use="required" type="xsd:string" />
              <xsd:attribute name="type" type="xsd:string" />
              <xsd:attribute name="mimetype" type="xsd:string" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="assembly">
            <xsd:complexType>
              <xsd:attribute name="alias" type="xsd:string" />
              <xsd:attribute name="name" type="xsd:string" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="data">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
                <xsd:element name="comment" type="xsd:string" minOccurs="0" msdata:Ordinal="2" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" msdata:Ordinal="1" />
              <xsd:attribute name="type" type="xsd:string" msdata:Ordinal="3" />
              <xsd:attribute name="mimetype" type="xsd:string" msdata:Ordinal="4" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="resheader">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" />
            </xsd:complexType>
          </xsd:element>
        </xsd:choice>
      </xsd:complexType>
    </xsd:element>
  </xsd:schema>
  <resheader name="resmimetype">
    <value>text/microsoft-resx</value>
  </resheader>
  <resheader name="version">
    <value>2.0</value>
  </resheader>
  <resheader name="reader">
    <value>System.Resources.ResXResourceReader, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <resheader name="writer">
    <value>System.Resources.ResXResourceWriter, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <data name="sqlDataSource1.ResultSchemaSerializable" xml:space="preserve">
    <value></value>
  </data>
  <data name="Total_RejEggs.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([RejBroken]) Or [RejBroken] == 0, 0, [RejBroken]) +
Iif(IsNullOrEmpty([RejCrack]) Or [RejCrack] == 0, 0, [RejCrack]) +
Iif(IsNullOrEmpty([RejDef]) Or [RejDef] == 0, 0, [RejDef]) +
Iif(IsNullOrEmpty([RejOther]) Or [RejOther] == 0, 0, [RejOther])
</value>
  </data>
  <data name="CumHatchability.Expression" xml:space="preserve">
    <value>Iif([SR_HT_FlkSumProdReport.SettedEggs] &lt;&gt; 0, 
    ([SR_HT_FlkSumProdReport.SelChicks] + [SR_HT_FlkSumProdReport.CullsChicks] + [SR_HT_FlkSumProdReport.DeadChicks]) / [SR_HT_FlkSumProdReport.SettedEggs] * 100, 
    0
)
</value>
  </data>
  <data name="TotalEggSold.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([SoldHatEggs]) Or [SoldHatEggs] == 0, 0, [SoldHatEggs]) +
Iif(IsNullOrEmpty([SoldTableEggs]) Or [SoldTableEggs] == 0, 0, [SoldTableEggs]) +
Iif(IsNullOrEmpty([EggsToOffal]) Or [EggsToOffal] == 0, 0, [EggsToOffal])
</value>
  </data>
  <data name="StockHatEggBal.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([BalOfHatEggs]) Or [BalOfHatEggs] == 0, 0, [BalOfHatEggs]) -
(
    Iif(IsNullOrEmpty([SR_HT_FlkSumProdReport.SettedEggs]) Or [SR_HT_FlkSumProdReport.SettedEggs] == 0, 0, [SR_HT_FlkSumProdReport.SettedEggs]) +
    Iif(IsNullOrEmpty([TotalEggSold]) Or [TotalEggSold] == 0, 0, [TotalEggSold])
)
</value>
  </data>
  <data name="ActHatchBalanceEggs.Expression" xml:space="preserve">
    <value>Iif(
    (IsNullOrEmpty([SetEggBalHatEgg]) Or [SetEggBalHatEgg] == 0), 
    0, 
    (Iif(IsNullOrEmpty([SelChickBalHatEgg]), 0, [SelChickBalHatEgg]) 
    + Iif(IsNullOrEmpty([CullsBalHatEgg]), 0, [CullsBalHatEgg]) 
    + Iif(IsNullOrEmpty([DeadsBalHatEgg]), 0, [DeadsBalHatEgg])) 
    / [SetEggBalHatEgg] * 100
)
</value>
  </data>
  <data name="Total_RejEggs_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([RejBroken_1]) Or [RejBroken_1] = 0
 Or IsNullOrEmpty([RejCrack_1]) Or [RejCrack_1] = 0
 Or IsNullOrEmpty([RejDef_1]) Or [RejDef_1] = 0
 Or IsNullOrEmpty([RejOther_1]) Or [RejOther_1] = 0,
 0,
 [RejBroken_1] + [RejCrack_1] + [RejDef_1] + [RejOther_1])
</value>
  </data>
  <data name="CumChicks_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([SelChicks_1]) Or [SelChicks_1] = 0
 Or IsNullOrEmpty([CullsChicks_1]) Or [CullsChicks_1] = 0
 Or IsNullOrEmpty([DeadChicks_1]) Or [DeadChicks_1] = 0,
 0,
 [SelChicks_1] + [CullsChicks_1] + [DeadChicks_1])
</value>
  </data>
  <data name="CumHatchability_1.Expression" xml:space="preserve">
    <value>Iif(
    IsNullOrEmpty([SettedEggs_1]) Or [SettedEggs_1] = 0,
    0,
    (
        Iif(IsNullOrEmpty([SelChicks_1]) Or [SelChicks_1] = 0, 0, [SelChicks_1]) +
        Iif(IsNullOrEmpty([CullsChicks_1]) Or [CullsChicks_1] = 0, 0, [CullsChicks_1]) +
        Iif(IsNullOrEmpty([DeadChicks_1]) Or [DeadChicks_1] = 0, 0, [DeadChicks_1])
    ) / [SettedEggs_1] * 100
)
</value>
  </data>
  <data name="SoldTableEggs_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([ER_SoldAsTableEggs_1]) Or [ER_SoldAsTableEggs_1] == 0, 0, [ER_SoldAsTableEggs_1]) 
+ Iif(IsNullOrEmpty([ES_SoldAsTableEggs_1]) Or [ES_SoldAsTableEggs_1] == 0, 0, [ES_SoldAsTableEggs_1])
</value>
  </data>
  <data name="EggsToOffal_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([SR_HT_FlkSumProdReport.ER_SendToOffal_1]) OR [SR_HT_FlkSumProdReport.ER_SendToOffal_1] == 0, 0, [SR_HT_FlkSumProdReport.ER_SendToOffal_1]) 
+ 
Iif(IsNullOrEmpty([SR_HT_FlkSumProdReport.ES_SendToOffal_1]) OR [SR_HT_FlkSumProdReport.ES_SendToOffal_1] == 0, 0, [SR_HT_FlkSumProdReport.ES_SendToOffal_1])
</value>
  </data>
  <data name="TotalEggSold_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([SoldHatEggs_1]) OR [SoldHatEggs_1] == 0, 0, [SoldHatEggs_1])
+
Iif(IsNullOrEmpty([SoldTableEggs_1]) OR [SoldTableEggs_1] == 0, 0, [SoldTableEggs_1])
+
Iif(IsNullOrEmpty([EggsToOffal_1]) OR [EggsToOffal_1] == 0, 0, [EggsToOffal_1])
</value>
  </data>
  <data name="CumChicks_Act_Est_1.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([EstChicks_PerEgg_1]) OR [EstChicks_PerEgg_1] == 0, 0, [EstChicks_PerEgg_1])
+
Iif(IsNullOrEmpty([ESTChicks_PerStock_HE_1]) OR [ESTChicks_PerStock_HE_1] == 0, 0, [ESTChicks_PerStock_HE_1])
+
Iif(IsNullOrEmpty([CumChicks_1]) OR [CumChicks_1] == 0, 0, [CumChicks_1])
</value>
  </data>
  <data name="AB_CumTotalChicksHH_VS_Act.Expression" xml:space="preserve">
    <value>Iif(
    [POL] &lt;&gt; 0,
    (
        Iif(IsNullOrEmpty([CumChicks]) OR [CumChicks] == 0, 0, [CumChicks])
        +
        Iif(IsNullOrEmpty([SelChicks_1]) OR [SelChicks_1] == 0, 0, [SelChicks_1])
        +
        Iif(IsNullOrEmpty([CullsChicks_1]) OR [CullsChicks_1] == 0, 0, [CullsChicks_1])
        +
        Iif(IsNullOrEmpty([DeadChicks_1]) OR [DeadChicks_1] == 0, 0, [DeadChicks_1])
    )
    / [POL],
    0
)
</value>
  </data>
  <data name="AB_RejChicksHH_VS_Act.Expression" xml:space="preserve">
    <value>Iif(
    [POL] &lt;&gt; 0,
    (
        Iif(IsNullOrEmpty([RejChicks]) OR [RejChicks] == 0, 0, [RejChicks])
        +
        Iif(IsNullOrEmpty([CullsChicks_1]) OR [CullsChicks_1] == 0, 0, [CullsChicks_1])
        +
        Iif(IsNullOrEmpty([DeadChicks_1]) OR [DeadChicks_1] == 0, 0, [DeadChicks_1])
    )
    / [POL],
    0
)
</value>
  </data>
  <data name="AB_CumAGradeChicksHH_VS_Act.Expression" xml:space="preserve">
    <value>Iif(
    [POL] &lt;&gt; 0,
    (
        Iif(IsNullOrEmpty([RejChicks]) OR [RejChicks] == 0, 0, [RejChicks])
        +
        Iif(IsNullOrEmpty([CullsChicks_1]) OR [CullsChicks_1] == 0, 0, [CullsChicks_1])
        +
        Iif(IsNullOrEmpty([DeadChicks_1]) OR [DeadChicks_1] == 0, 0, [DeadChicks_1])
    )
    / [POL],
    0
)
</value>
  </data>
  <data name="AB_CumTotalChicksHH.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([CumChicks_ActEst_HH]) OR [CumChicks_ActEst_HH] == 0, 0, [CumChicks_ActEst_HH])
+
Iif(IsNullOrEmpty([CumChicks_ActEst_HH_1]) OR [CumChicks_ActEst_HH_1] == 0, 0, [CumChicks_ActEst_HH_1])
</value>
  </data>
  <data name="Data_2.Expression" xml:space="preserve">
    <value>Iif(?IsRTL,[Data] + ' (' + [After] + ' ' + ToStr([DaysNO]) + ' ' + [Day] + ') ' + [Data] + ToStr([SR_HT_FlkSumProdReport.LastDays]) + ' ' + [Day] + ' ' + [Only]
 , [Data] + ' (' + [After] + ' ' + ToStr([DaysNO]) + ' ' + [Days] + ') ' + [Data] + + [Only]+ ToStr([SR_HT_FlkSumProdReport.LastDays]) + ' ' + [Days] 
)</value>
  </data>
  <data name="Perf2.Expression" xml:space="preserve">
    <value>Iif(?IsRTL,
    [Performance] + ' (' + [After] + ' ' + ToStr([DaysNO]) + ' ' + [Day] + ') ' + [Data] + ToStr([SR_HT_FlkSumProdReport.LastDays]) + ' ' + [Day] + ' ' + [Only],
    'Performance (After ' + ToStr([DaysNO]) + ' days) Data only ' + ToStr([SR_HT_FlkSumProdReport.LastDays]) + ' days'
)
</value>
  </data>
  <data name="ActHatchBalanceEggs_1.Expression" xml:space="preserve">
    <value>Iif([SR_HT_FlkSumProdReport.SetEggBalHatEgg_1] &lt;&gt; 0, 
    ([SR_HT_FlkSumProdReport.SelChickBalHatEgg_1] + [SR_HT_FlkSumProdReport.CullsBalHatEgg_1] + [SR_HT_FlkSumProdReport.DeadsBalHatEgg_1]) / [SR_HT_FlkSumProdReport.SetEggBalHatEgg_1] * 100, 
    0
)
</value>
  </data>
  <data name="SoldTableEggs_1_FromPartA.Expression" xml:space="preserve">
    <value>Iif(IsNullOrEmpty([ER_SoldAsTableEggs_1]) Or [ER_SoldAsTableEggs_1] == 0, 0, [ER_SoldAsTableEggs_1]) 
+ Iif(IsNullOrEmpty([ES_SoldAsTableEggs_1]) Or [ES_SoldAsTableEggs_1] == 0, 0, [ES_SoldAsTableEggs_1])
</value>
  </data>
  <data name="$this.ScriptsSource" xml:space="preserve">
    <value>// Declare at report level (top of the report's class)
decimal total = 0;
decimal total_1 = 0;
decimal RejDef  = 0;
decimal RejCrack  = 0;
// Report-level fields (no access modifier)
decimal RejBroken = 0;
decimal RejOther = 0;
decimal TotalRej = 0;
decimal BalanceOfHatEggs = 0;
decimal TotalChicks = 0;
decimal cumHatchability = 0;

private void xrTableCell180_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    var femalesTrans = Convert.ToDecimal(GetCurrentColumnValue("FemalesTrans"));
    var femalesDepleted = Convert.ToDecimal(GetCurrentColumnValue("FemalesDepleted"));

    if (femalesTrans != 0)
        ((XRTableCell)sender).Text = ((femalesDepleted / femalesTrans) * 100).ToString("N2");
    else
        ((XRTableCell)sender).Text = "N2";
}

private void xrTableCell38_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal htRejDeformed = 0;
decimal stRejDeformed = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejDeformed") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejDeformed").ToString()))
{
    htRejDeformed = Convert.ToDecimal(GetCurrentColumnValue("HT_RejDeformed"));
}

if (GetCurrentColumnValue("ST_RejDeformed") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejDeformed").ToString()))
{
    stRejDeformed = Convert.ToDecimal(GetCurrentColumnValue("ST_RejDeformed"));
}

// --- Equation ---
RejDef = htRejDeformed + stRejDeformed;


// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = RejDef.ToString("N0");   // format with 2 decimals

}

private void xrTableCell41_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal htRejCracks = 0;
decimal stRejCracks = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejCracks") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejCracks").ToString()))
{
    htRejCracks = Convert.ToDecimal(GetCurrentColumnValue("HT_RejCracks"));
}

if (GetCurrentColumnValue("ST_RejCracks") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejCracks").ToString()))
{
    stRejCracks = Convert.ToDecimal(GetCurrentColumnValue("ST_RejCracks"));
}

// --- Equation ---
RejCrack = htRejCracks + stRejCracks;


// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = RejCrack.ToString("N0");   // format with 2 decimals

}

private void xrTableCell42_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal htRejBroken = 0;
decimal stRejBroken = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejBroken") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejBroken").ToString()))
{
    htRejBroken = Convert.ToDecimal(GetCurrentColumnValue("HT_RejBroken"));
}

if (GetCurrentColumnValue("ST_RejBroken") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejBroken").ToString()))
{
    stRejBroken = Convert.ToDecimal(GetCurrentColumnValue("ST_RejBroken"));
}

// --- Equation ---
RejBroken = htRejBroken + stRejBroken;


// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = RejBroken.ToString("N2");   // format with 2 decimals

}

private void xrTableCell45_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal htRejOther = 0;
decimal stRejOther = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejOther") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejOther").ToString()))
{
    htRejOther = Convert.ToDecimal(GetCurrentColumnValue("HT_RejOther"));
}

if (GetCurrentColumnValue("ST_RejOther") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejOther").ToString()))
{
    stRejOther = Convert.ToDecimal(GetCurrentColumnValue("ST_RejOther"));
}

// --- Equation ---
RejOther  = htRejOther + stRejOther;


// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = RejOther.ToString("N2");   // format with 2 decimals

}

private void xrTableCell47_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal rejBroken = 0;
decimal rejCrack = 0;
decimal rejDef = 0;
decimal rejOther = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RejBroken") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejBroken").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) != 0)
{
    rejBroken = Convert.ToDecimal(GetCurrentColumnValue("RejBroken"));
}

if (GetCurrentColumnValue("RejCrack") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejCrack").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejCrack")) != 0)
{
    rejCrack = Convert.ToDecimal(GetCurrentColumnValue("RejCrack"));
}

if (GetCurrentColumnValue("RejDef") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejDef").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejDef")) != 0)
{
    rejDef = Convert.ToDecimal(GetCurrentColumnValue("RejDef"));
}

if (GetCurrentColumnValue("RejOther") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejOther").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejOther")) != 0)
{
    rejOther = Convert.ToDecimal(GetCurrentColumnValue("RejOther"));
}

// --- Equation ---
TotalRej = rejBroken + rejCrack + rejDef + rejOther;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = TotalRej.ToString("N0");   // format with 2 decimals

}

private void xrTableCell49_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal farmInTake = 0;
decimal rejBroken = 0;
decimal rejCrack = 0;
decimal rejDef = 0;
decimal rejOther = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("FarmInTake") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("FarmInTake").ToString()))
{
    farmInTake = Convert.ToDecimal(GetCurrentColumnValue("FarmInTake"));
}

if (GetCurrentColumnValue("RejBroken") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejBroken").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) != 0)
{
    rejBroken = Convert.ToDecimal(GetCurrentColumnValue("RejBroken"));
}

if (GetCurrentColumnValue("RejCrack") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejCrack").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejCrack")) != 0)
{
    rejCrack = Convert.ToDecimal(GetCurrentColumnValue("RejCrack"));
}

if (GetCurrentColumnValue("RejDef") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejDef").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejDef")) != 0)
{
    rejDef = Convert.ToDecimal(GetCurrentColumnValue("RejDef"));
}

if (GetCurrentColumnValue("RejOther") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejOther").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejOther")) != 0)
{
    rejOther = Convert.ToDecimal(GetCurrentColumnValue("RejOther"));
}

// --- Equation ---
decimal totalRej = rejBroken + rejCrack + rejDef + rejOther;
BalanceOfHatEggs = farmInTake - totalRej;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = BalanceOfHatEggs.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell55_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
// --- Declare values ---
decimal selChicks = 0;
decimal cullsChicks = 0;
decimal deadChicks = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("SelChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("SelChicks").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("SelChicks")) != 0)
{
    selChicks = Convert.ToDecimal(GetCurrentColumnValue("SelChicks"));
}

if (GetCurrentColumnValue("CullsChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("CullsChicks")) != 0)
{
    cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"));
}

if (GetCurrentColumnValue("DeadChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("DeadChicks")) != 0)
{
    deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"));
}

// --- Equation ---
TotalChicks = selChicks + cullsChicks + deadChicks;


// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = TotalChicks.ToString("N0");   // format with 2 decimals

}

private void xrTableCell61_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal cullsChicks = 0;
decimal deadChicks = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CullsChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("CullsChicks")) != 0)
{
    cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"));
}

if (GetCurrentColumnValue("DeadChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("DeadChicks")) != 0)
{
    deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"));
}

// --- Equation ---
decimal totalChicks = cullsChicks + deadChicks;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalChicks.ToString("0.##");   // format with 2 decimals

}


private void xrTableCell57_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsHatEggs = 0;
decimal esSoldAsHatEggs = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsHatEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs")) != 0)
{
    erSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs"));
}

if (GetCurrentColumnValue("ES_SoldAsHatEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs")) != 0)
{
    esSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs"));
}

// --- Equation ---
decimal totalSoldAsHatEggs = erSoldAsHatEggs + esSoldAsHatEggs;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalSoldAsHatEggs.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell63_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsTableEggs = 0;
decimal esSoldAsTableEggs = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsTableEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs")) != 0)
{
    erSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs"));
}

if (GetCurrentColumnValue("ES_SoldAsTableEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs")) != 0)
{
    esSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs"));
}

// --- Equation ---
decimal totalSoldAsTableEggs = erSoldAsTableEggs + esSoldAsTableEggs;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalSoldAsTableEggs.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell65_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSendToOffal = 0;
decimal esSendToOffal = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SendToOffal") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal")) != 0)
{
    erSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal"));
}

if (GetCurrentColumnValue("ES_SendToOffal") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal")) != 0)
{
    esSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal"));
}

// --- Equation ---
decimal totalSendToOffal = erSendToOffal + esSendToOffal;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalSendToOffal.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell68_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
    decimal erSoldAsHatEggs = 0;
    decimal esSoldAsHatEggs = 0;
    decimal erSoldAsTableEggs = 0;
    decimal esSoldAsTableEggs = 0;
    decimal erSendToOffal = 0;
    decimal esSendToOffal = 0;

    // --- Read values safely ---
    if (GetCurrentColumnValue("ER_SoldAsHatEggs") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs")) != 0)
    {
        erSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs"));
    }

    if (GetCurrentColumnValue("ES_SoldAsHatEggs") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs")) != 0)
    {
        esSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs"));
    }

    if (GetCurrentColumnValue("ER_SoldAsTableEggs") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs")) != 0)
    {
        erSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs"));
    }

    if (GetCurrentColumnValue("ES_SoldAsTableEggs") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs")) != 0)
    {
        esSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs"));
    }

    if (GetCurrentColumnValue("ER_SendToOffal") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal")) != 0)
    {
        erSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal"));
    }

    if (GetCurrentColumnValue("ES_SendToOffal") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal")) != 0)
    {
        esSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal"));
    }

    // --- Equation ---
    total = (erSoldAsHatEggs + esSoldAsHatEggs) +
                    (erSoldAsTableEggs + esSoldAsTableEggs) +
                    (erSendToOffal + esSendToOffal);

    // --- Assign result to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = total.ToString("0.##");   // format with 2 decimals


}

private void xrTableCell71_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal selChicks = 0;
decimal cullsChicks = 0;
decimal deadChicks = 0;
decimal settedEggs = 0;

// --- Read values directly ---
if (GetCurrentColumnValue("SelChicks") != null)
    selChicks = Convert.ToDecimal(GetCurrentColumnValue("SelChicks"));

if (GetCurrentColumnValue("CullsChicks") != null)
    cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"));

if (GetCurrentColumnValue("DeadChicks") != null)
    deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"));

if (GetCurrentColumnValue("SettedEggs") != null)
    settedEggs = Convert.ToDecimal(GetCurrentColumnValue("SettedEggs"));

// --- Calculate CumHatchability ---
decimal cumHatchability = 0;
if (settedEggs != 0)
    cumHatchability = (selChicks + cullsChicks + deadChicks) / settedEggs * 100m;

decimal result =( total * cumHatchability ) / settedEggs  * 100m
// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##"); // format with 2 decimals

}

private void xrTableCell74_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- FarmInTake ---
    decimal farmInTake = 0;
    object valFarm = GetCurrentColumnValue("FarmInTake");
    if (valFarm != null &amp;&amp; decimal.TryParse(valFarm.ToString(), out decimal tempFarm))
        farmInTake = tempFarm;

    // --- Total_RejEggs ---
    decimal totalRejEggs = 0;
    object valRej = GetCurrentColumnValue("Total_RejEggs");
    if (valRej != null &amp;&amp; decimal.TryParse(valRej.ToString(), out decimal tempRej))
        totalRejEggs = tempRej;

    // --- BalOfHatEggs = FarmInTake - Total_RejEggs ---
    decimal balOfHatEggs = farmInTake - totalRejEggs;

    // --- SettedEggs ---
    decimal settedEggs = 0;
    object valSet = GetCurrentColumnValue("SettedEggs");
    if (valSet != null &amp;&amp; decimal.TryParse(valSet.ToString(), out decimal tempSet))
        settedEggs = tempSet;

    // --- Final equation ---
    decimal result = balOfHatEggs - (settedEggs + total);

    // --- Assign result to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##");
}

private void xrTableCell77_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // --- StockHatEggBal ---
    decimal stockHatEggBal = 0;
    object valStock = GetCurrentColumnValue("StockHatEggBal");
    if (valStock != null &amp;&amp; decimal.TryParse(valStock.ToString(), out decimal tempStock) &amp;&amp; tempStock != 0)
        stockHatEggBal = tempStock;

    // --- SetEggBalHatEgg ---
    decimal setEggBalHatEgg = 0;
    object valSetEgg = GetCurrentColumnValue("SetEggBalHatEgg");
    if (valSetEgg != null &amp;&amp; decimal.TryParse(valSetEgg.ToString(), out decimal tempSetEgg))
        setEggBalHatEgg = tempSetEgg;

    // --- ActHatchBalanceEggs ---
    decimal actHatchBalanceEggs = 0;
    if (setEggBalHatEgg != 0)
    {
        decimal selChick = 0, culls = 0, deads = 0;

        object valSel = GetCurrentColumnValue("SelChickBalHatEgg");
        if (valSel != null &amp;&amp; decimal.TryParse(valSel.ToString(), out decimal tempSel))
            selChick = tempSel;

        object valCulls = GetCurrentColumnValue("CullsBalHatEgg");
        if (valCulls != null &amp;&amp; decimal.TryParse(valCulls.ToString(), out decimal tempCulls))
            culls = tempCulls;

        object valDeads = GetCurrentColumnValue("DeadsBalHatEgg");
        if (valDeads != null &amp;&amp; decimal.TryParse(valDeads.ToString(), out decimal tempDeads))
            deads = tempDeads;

        actHatchBalanceEggs = (selChick + culls + deads) / setEggBalHatEgg * 100;
    }

    // --- Final equation ---
    decimal result = stockHatEggBal * actHatchBalanceEggs / 100;

    // --- Assign result ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##");
}

private void xrTableCell80_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        decimal result = 0;

    // --- Read SetEggBalHatEgg ---
    object valSetEgg = GetCurrentColumnValue("SetEggBalHatEgg");
    if (valSetEgg != null &amp;&amp; decimal.TryParse(valSetEgg.ToString(), out decimal setEggBalHatEgg) &amp;&amp; setEggBalHatEgg != 0)
    {
        // --- Read SelChickBalHatEgg ---
        decimal selChick = 0;
        object valSel = GetCurrentColumnValue("SelChickBalHatEgg");
        if (valSel != null &amp;&amp; decimal.TryParse(valSel.ToString(), out decimal tempSel))
            selChick = tempSel;

        // --- Read CullsBalHatEgg ---
        decimal culls = 0;
        object valCulls = GetCurrentColumnValue("CullsBalHatEgg");
        if (valCulls != null &amp;&amp; decimal.TryParse(valCulls.ToString(), out decimal tempCulls))
            culls = tempCulls;

        // --- Read DeadsBalHatEgg ---
        decimal deads = 0;
        object valDeads = GetCurrentColumnValue("DeadsBalHatEgg");
        if (valDeads != null &amp;&amp; decimal.TryParse(valDeads.ToString(), out decimal tempDeads))
            deads = tempDeads;

        // --- Equation ---
        result = (selChick + culls + deads) / setEggBalHatEgg * 100;
    }

    // --- Assign to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##");
}

private void xrTableCell83_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    decimal result = 0;

    // ------------------ EstChicks_PerEgg ------------------
    decimal estChicks_PerEgg = 0;
    decimal cumHatchability = 0;

    object valSetted = GetCurrentColumnValue("SettedEggs");
    if (valSetted != null &amp;&amp; decimal.TryParse(valSetted.ToString(), out decimal settedEggs) &amp;&amp; settedEggs != 0)
    {
        // Sel + Culls + Deads
        decimal sel = 0, culls = 0, dead = 0;

        if (decimal.TryParse(GetCurrentColumnValue("SelChicks")?.ToString(), out decimal tempSel)) sel = tempSel;
        if (decimal.TryParse(GetCurrentColumnValue("CullsChicks")?.ToString(), out decimal tempCulls)) culls = tempCulls;
        if (decimal.TryParse(GetCurrentColumnValue("DeadChicks")?.ToString(), out decimal tempDead)) dead = tempDead;

        cumHatchability = (sel + culls + dead) / settedEggs * 100;
    }

    // use global "total" for TotalEggSold
    estChicks_PerEgg = total * cumHatchability / 100;

    // ------------------ ESTChicks_PerStock_HE ------------------
    decimal estChicks_PerStock_HE = 0;

    if (decimal.TryParse(GetCurrentColumnValue("StockHatEggBal")?.ToString(), out decimal stockBal) &amp;&amp; stockBal != 0)
    {
        // Calculate ActHatchBalanceEggs
        decimal actHatchBalanceEggs = 0;
        if (decimal.TryParse(GetCurrentColumnValue("SetEggBalHatEgg")?.ToString(), out decimal setEggBal) &amp;&amp; setEggBal != 0)
        {
            decimal selBal = 0, cullsBal = 0, deadBal = 0;

            if (decimal.TryParse(GetCurrentColumnValue("SelChickBalHatEgg")?.ToString(), out decimal tempSelBal)) selBal = tempSelBal;
            if (decimal.TryParse(GetCurrentColumnValue("CullsBalHatEgg")?.ToString(), out decimal tempCullsBal)) cullsBal = tempCullsBal;
            if (decimal.TryParse(GetCurrentColumnValue("DeadsBalHatEgg")?.ToString(), out decimal tempDeadBal)) deadBal = tempDeadBal;

            actHatchBalanceEggs = (selBal + cullsBal + deadBal) / setEggBal * 100;
        }

        estChicks_PerStock_HE = stockBal * actHatchBalanceEggs / 100;
    }

    // ------------------ CumChicks ------------------
    decimal cumChicks = 0;
    if (decimal.TryParse(GetCurrentColumnValue("CumChicks")?.ToString(), out decimal tempCum))
        cumChicks = tempCum;

    // ------------------ Final Equation ------------------
    result = estChicks_PerEgg + estChicks_PerStock_HE + cumChicks;

    // Assign to cell
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##");
}

private void xrTableCell73_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     decimal farmInTake = GetCurrentColumnValue("FarmInTake") != null ? Convert.ToDecimal(GetCurrentColumnValue("FarmInTake")) : 0;
    decimal pol = GetCurrentColumnValue("POL") != null ? Convert.ToDecimal(GetCurrentColumnValue("POL")) : 0;

    if (pol != 0)
        ((XRTableCell)sender).Text = (farmInTake / pol).ToString("0.00"); // formatted to 2 decimals
    else
        ((XRTableCell)sender).Text = "0";
}

private void xrTableCell95_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // Get values safely with null checks
    decimal pol = GetCurrentColumnValue("POL") != null ? Convert.ToDecimal(GetCurrentColumnValue("POL")) : 0;

    decimal rejBroken = GetCurrentColumnValue("RejBroken") != null ? Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) : 0;
    decimal rejCrack  = GetCurrentColumnValue("RejCrack")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejCrack"))  : 0;
    decimal rejDef    = GetCurrentColumnValue("RejDef")    != null ? Convert.ToDecimal(GetCurrentColumnValue("RejDef"))    : 0;
    decimal rejOther  = GetCurrentColumnValue("RejOther")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejOther"))  : 0;

    // Total rejected eggs
    decimal totalRejEggs = rejBroken + rejCrack + rejDef + rejOther;

    // Final calculation
    decimal result = (pol == 0) ? 0 : (totalRejEggs / pol);

    // Assign to cell text (formatted with 2 decimals)
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell98_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // Get POL
    decimal pol = GetCurrentColumnValue("POL") != null ? Convert.ToDecimal(GetCurrentColumnValue("POL")) : 0;

    // FarmInTake
    decimal farmInTake = GetCurrentColumnValue("FarmInTake") != null ? Convert.ToDecimal(GetCurrentColumnValue("FarmInTake")) : 0;

    // Rejections
    decimal rejBroken = GetCurrentColumnValue("RejBroken") != null ? Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) : 0;
    decimal rejCrack  = GetCurrentColumnValue("RejCrack")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejCrack"))  : 0;
    decimal rejDef    = GetCurrentColumnValue("RejDef")    != null ? Convert.ToDecimal(GetCurrentColumnValue("RejDef"))    : 0;
    decimal rejOther  = GetCurrentColumnValue("RejOther")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejOther"))  : 0;

    // Total rejected eggs
    decimal totalRejEggs = rejBroken + rejCrack + rejDef + rejOther;

    // BalOfHatEggs = FarmInTake - Total_RejEggs
    decimal balOfHatEggs = farmInTake - totalRejEggs;

    // Final calculation
    decimal result = (pol == 0) ? 0 : (balOfHatEggs / pol);

    // Assign result to the cell (2 decimals formatting)
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell104_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // Get POL
    decimal pol = GetCurrentColumnValue("POL") != null ? Convert.ToDecimal(GetCurrentColumnValue("POL")) : 0;

    // Get CumChicks
    decimal cumChicks = GetCurrentColumnValue("CumChicks") != null ? Convert.ToDecimal(GetCurrentColumnValue("CumChicks")) : 0;

    // Final calculation
    decimal result = (pol == 0) ? 0 : (cumChicks / pol);

    // Assign result to the cell (formatted with 2 decimals)
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell107_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // Get values from dataset
    decimal settedEggs = GetCurrentColumnValue("SettedEggs") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SettedEggs"))
        : 0;

    decimal selChicks = GetCurrentColumnValue("SelChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
        : 0;

    decimal cullsChicks = GetCurrentColumnValue("CullsChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"))
        : 0;

    decimal deadChicks = GetCurrentColumnValue("DeadChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
        : 0;

    // Compute CumHatchability
    decimal result = (settedEggs != 0)
        ? ((selChicks + cullsChicks + deadChicks) / settedEggs * 100)
        : 0;

    // Assign result to the XRTableCell
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell85_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // Get values
    decimal pol = GetCurrentColumnValue("POL") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("POL"))
        : 0;

    decimal rejChicks = GetCurrentColumnValue("RejChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("RejChicks"))
        : 0;

    // Compute result
    decimal result = (pol != 0) ? (rejChicks / pol) : 0;

    // Assign result to cell
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell94_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // Get values
    decimal pol = GetCurrentColumnValue("POL") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("POL"))
        : 0;

    decimal selChicks = GetCurrentColumnValue("SelChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
        : 0;

    // Compute result
    decimal result = (pol != 0) ? (selChicks / pol) : 0;

    // Assign result to the table cell
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell118_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // Read POL
    decimal pol = GetCurrentColumnValue("POL") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("POL"))
        : 0;

    // Read TotalEggSold (from global variable "total")
    decimal totalEggSold = total;  // &lt;--- as you said, global from another script

    // Compute CumHatchability
    decimal settedEggs = GetCurrentColumnValue("SettedEggs") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SettedEggs"))
        : 0;
    decimal selChicks = GetCurrentColumnValue("SelChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
        : 0;
    decimal cullsChicks = GetCurrentColumnValue("CullsChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"))
        : 0;
    decimal deadChicks = GetCurrentColumnValue("DeadChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
        : 0;

    decimal cumHatchability = (settedEggs != 0)
        ? ((selChicks + cullsChicks + deadChicks) / settedEggs) * 100
        : 0;

    // Compute EstChicks_PerEgg
    decimal estChicksPerEgg = (totalEggSold != 0 &amp;&amp; cumHatchability != 0)
        ? (totalEggSold * cumHatchability / 100)
        : 0;

    // Final formula: divide by POL
    decimal result = (pol != 0) ? (estChicksPerEgg / pol) : 0;

    // Assign result
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell124_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- POL ---
    decimal pol = GetCurrentColumnValue("POL") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("POL"))
        : 0;

    // --- BalOfHatEggs = FarmInTake - Total_RejEggs ---
    decimal farmInTake = GetCurrentColumnValue("FarmInTake") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("FarmInTake"))
        : 0;

    decimal rejEggs = 0;
    decimal rejBroken = GetCurrentColumnValue("RejBroken") != null ? Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) : 0;
    decimal rejCrack  = GetCurrentColumnValue("RejCrack")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejCrack"))  : 0;
    decimal rejDef    = GetCurrentColumnValue("RejDef")    != null ? Convert.ToDecimal(GetCurrentColumnValue("RejDef"))    : 0;
    decimal rejOther  = GetCurrentColumnValue("RejOther")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejOther"))  : 0;
    rejEggs = rejBroken + rejCrack + rejDef + rejOther;

    decimal balOfHatEggs = farmInTake - rejEggs;

    // --- TotalEggSold (global from another script) ---
    decimal totalEggSold = total;

    // --- StockHatEggBal ---
    decimal settedEggs = GetCurrentColumnValue("SR_HT_FlkSumProdReport.SettedEggs") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport.SettedEggs"))
        : 0;

    decimal stockHatEggBal = balOfHatEggs - (settedEggs + totalEggSold);

    // --- ActHatchBalanceEggs ---
    decimal setEggBalHatEgg = GetCurrentColumnValue("SetEggBalHatEgg") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SetEggBalHatEgg"))
        : 0;
    decimal selChickBal = GetCurrentColumnValue("SelChickBalHatEgg") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChickBalHatEgg"))
        : 0;
    decimal cullsBal = GetCurrentColumnValue("CullsBalHatEgg") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("CullsBalHatEgg"))
        : 0;
    decimal deadsBal = GetCurrentColumnValue("DeadsBalHatEgg") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("DeadsBalHatEgg"))
        : 0;

    decimal actHatchBalanceEggs = (setEggBalHatEgg != 0)
        ? ((selChickBal + cullsBal + deadsBal) / setEggBalHatEgg) * 100
        : 0;

    // --- ESTChicks_PerStock_HE ---
    decimal estChicksPerStockHE = (stockHatEggBal != 0 &amp;&amp; actHatchBalanceEggs != 0)
        ? (stockHatEggBal * actHatchBalanceEggs / 100)
        : 0;

    // --- Final Result ---
    decimal result = (pol != 0) ? (estChicksPerStockHE / pol) : 0;

    // --- Assign to cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell127_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- POL ---
    decimal pol = GetCurrentColumnValue("POL") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("POL"))
        : 0;

    // --- BalOfHatEggs = FarmInTake - RejectedEggs ---
    decimal farmInTake = GetCurrentColumnValue("FarmInTake") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("FarmInTake"))
        : 0;

    decimal rejBroken = GetCurrentColumnValue("RejBroken") != null ? Convert.ToDecimal(GetCurrentColumnValue("RejBroken")) : 0;
    decimal rejCrack  = GetCurrentColumnValue("RejCrack")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejCrack"))  : 0;
    decimal rejDef    = GetCurrentColumnValue("RejDef")    != null ? Convert.ToDecimal(GetCurrentColumnValue("RejDef"))    : 0;
    decimal rejOther  = GetCurrentColumnValue("RejOther")  != null ? Convert.ToDecimal(GetCurrentColumnValue("RejOther"))  : 0;

    decimal balOfHatEggs = farmInTake - (rejBroken + rejCrack + rejDef + rejOther);

    // --- TotalEggSold (already global) ---
    decimal totalEggSold = total;

    // --- Setted Eggs ---
    decimal settedEggs = GetCurrentColumnValue("SR_HT_FlkSumProdReport.SettedEggs") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport.SettedEggs"))
        : 0;

    // --- CumHatchability ---
    decimal selChicks  = GetCurrentColumnValue("SR_HT_FlkSumProdReport.SelChicks") != null ? Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport.SelChicks")) : 0;
    decimal cullsChicks= GetCurrentColumnValue("SR_HT_FlkSumProdReport.CullsChicks") != null ? Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport.CullsChicks")) : 0;
    decimal deadChicks = GetCurrentColumnValue("SR_HT_FlkSumProdReport.DeadChicks") != null ? Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport.DeadChicks")) : 0;

    decimal cumHatchability = (settedEggs != 0)
        ? ((selChicks + cullsChicks + deadChicks) / settedEggs) * 100
        : 0;

    // --- EstChicks_PerEgg ---
    decimal estChicksPerEgg = (totalEggSold != 0 &amp;&amp; cumHatchability != 0)
        ? (totalEggSold * cumHatchability / 100)
        : 0;

    // --- StockHatEggBal ---
    decimal stockHatEggBal = balOfHatEggs - (settedEggs + totalEggSold);

    // --- ActHatchBalanceEggs ---
    decimal setEggBalHatEgg = GetCurrentColumnValue("SetEggBalHatEgg") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SetEggBalHatEgg"))
        : 0;
    decimal selChickBal = GetCurrentColumnValue("SelChickBalHatEgg") != null ? Convert.ToDecimal(GetCurrentColumnValue("SelChickBalHatEgg")) : 0;
    decimal cullsBal    = GetCurrentColumnValue("CullsBalHatEgg") != null ? Convert.ToDecimal(GetCurrentColumnValue("CullsBalHatEgg")) : 0;
    decimal deadsBal    = GetCurrentColumnValue("DeadsBalHatEgg") != null ? Convert.ToDecimal(GetCurrentColumnValue("DeadsBalHatEgg")) : 0;

    decimal actHatchBalanceEggs = (setEggBalHatEgg != 0)
        ? ((selChickBal + cullsBal + deadsBal) / setEggBalHatEgg) * 100
        : 0;

    // --- ESTChicks_PerStock_HE ---
    decimal estChicksPerStockHE = (stockHatEggBal != 0 &amp;&amp; actHatchBalanceEggs != 0)
        ? (stockHatEggBal * actHatchBalanceEggs / 100)
        : 0;

    // --- CumChicks (direct column) ---
    decimal cumChicks = GetCurrentColumnValue("CumChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("CumChicks"))
        : 0;

    // --- CumChicks_Act_Est ---
    decimal cumChicksActEst = estChicksPerEgg + estChicksPerStockHE + cumChicks;

    // --- Final Result ---
    decimal result = (pol != 0) ? (cumChicksActEst / pol) : 0;

    // --- Assign to cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");
}

private void xrTableCell148_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
 // --- Declare values ---
decimal htRejDeformed_1 = 0;
decimal stRejDeformed_1 = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejDeformed_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejDeformed_1").ToString()))
{
    htRejDeformed_1 = Convert.ToDecimal(GetCurrentColumnValue("HT_RejDeformed_1"));
}

if (GetCurrentColumnValue("ST_RejDeformed_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejDeformed_1").ToString()))
{
    stRejDeformed_1 = Convert.ToDecimal(GetCurrentColumnValue("ST_RejDeformed_1"));
}

// --- Equation ---
decimal result = htRejDeformed_1 + stRejDeformed_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell150_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
 // --- Declare values ---
decimal htRejCracks_1 = 0;
decimal stRejCracks_1 = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejCracks_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejCracks_1").ToString()))
{
    htRejCracks_1 = Convert.ToDecimal(GetCurrentColumnValue("HT_RejCracks_1"));
}

if (GetCurrentColumnValue("ST_RejCracks_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejCracks_1").ToString()))
{
    stRejCracks_1 = Convert.ToDecimal(GetCurrentColumnValue("ST_RejCracks_1"));
}

// --- Equation ---
decimal result = htRejCracks_1 + stRejCracks_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell152_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
 // --- Declare values ---
decimal htRejBroken_1 = 0;
decimal stRejBroken_1 = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejBroken_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejBroken_1").ToString()))
{
    htRejBroken_1 = Convert.ToDecimal(GetCurrentColumnValue("HT_RejBroken_1"));
}

if (GetCurrentColumnValue("ST_RejBroken_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejBroken_1").ToString()))
{
    stRejBroken_1 = Convert.ToDecimal(GetCurrentColumnValue("ST_RejBroken_1"));
}

// --- Equation ---
decimal result = htRejBroken_1 + stRejBroken_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell154_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal htRejOther_1 = 0;
decimal stRejOther_1 = 0;

// --- Read values ---
if (GetCurrentColumnValue("HT_RejOther_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("HT_RejOther_1").ToString()))
{
    htRejOther_1 = Convert.ToDecimal(GetCurrentColumnValue("HT_RejOther_1"));
}

if (GetCurrentColumnValue("ST_RejOther_1") != null &amp;&amp; 
    !string.IsNullOrEmpty(GetCurrentColumnValue("ST_RejOther_1").ToString()))
{
    stRejOther_1 = Convert.ToDecimal(GetCurrentColumnValue("ST_RejOther_1"));
}

// --- Equation ---
decimal result = htRejOther_1 + stRejOther_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell156_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values --- 
decimal rejBroken_1 = 0;
decimal rejCrack_1 = 0;
decimal rejDef_1 = 0;
decimal rejOther_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RejBroken_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejBroken_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejBroken_1")) != 0)
{
    rejBroken_1 = Convert.ToDecimal(GetCurrentColumnValue("RejBroken_1"));
}

if (GetCurrentColumnValue("RejCrack_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejCrack_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejCrack_1")) != 0)
{
    rejCrack_1 = Convert.ToDecimal(GetCurrentColumnValue("RejCrack_1"));
}

if (GetCurrentColumnValue("RejDef_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejDef_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejDef_1")) != 0)
{
    rejDef_1 = Convert.ToDecimal(GetCurrentColumnValue("RejDef_1"));
}

if (GetCurrentColumnValue("RejOther_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejOther_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejOther_1")) != 0)
{
    rejOther_1 = Convert.ToDecimal(GetCurrentColumnValue("RejOther_1"));
}

// --- Equation ---
decimal result = rejBroken_1 + rejCrack_1 + rejDef_1 + rejOther_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell158_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal farmInTake_1 = 0;
decimal rejBroken_1 = 0;
decimal rejCrack_1 = 0;
decimal rejDef_1 = 0;
decimal rejOther_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("FarmInTake_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("FarmInTake_1").ToString()))
{
    farmInTake_1 = Convert.ToDecimal(GetCurrentColumnValue("FarmInTake_1"));
}

if (GetCurrentColumnValue("RejBroken_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejBroken_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejBroken_1")) != 0)
{
    rejBroken_1 = Convert.ToDecimal(GetCurrentColumnValue("RejBroken_1"));
}

if (GetCurrentColumnValue("RejCrack_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejCrack_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejCrack_1")) != 0)
{
    rejCrack_1 = Convert.ToDecimal(GetCurrentColumnValue("RejCrack_1"));
}

if (GetCurrentColumnValue("RejDef_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejDef_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejDef_1")) != 0)
{
    rejDef_1 = Convert.ToDecimal(GetCurrentColumnValue("RejDef_1"));
}

if (GetCurrentColumnValue("RejOther_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejOther_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejOther_1")) != 0)
{
    rejOther_1 = Convert.ToDecimal(GetCurrentColumnValue("RejOther_1"));
}

// --- Equation ---
decimal totalRej_1 = rejBroken_1 + rejCrack_1 + rejDef_1 + rejOther_1;
decimal result_1 = farmInTake_1 - totalRej_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result_1.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell160_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal selChicks_1 = 0;
decimal cullsChicks_1 = 0;
decimal deadChicks_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("SelChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("SelChicks_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("SelChicks_1")) != 0)
{
    selChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("SelChicks_1"));
}

if (GetCurrentColumnValue("CullsChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("CullsChicks_1")) != 0)
{
    cullsChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks_1"));
}

if (GetCurrentColumnValue("DeadChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("DeadChicks_1")) != 0)
{
    deadChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks_1"));
}

// --- Equation ---
decimal totalChicks_1 = selChicks_1 + cullsChicks_1 + deadChicks_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalChicks_1.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell162_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- Declare values ---
    decimal cullsChicks = 0;
    decimal deadChicks = 0;

    // --- Read values safely ---
    if (GetCurrentColumnValue("CullsChicks") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("CullsChicks")) != 0)
    {
        cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"));
    }

    if (GetCurrentColumnValue("DeadChicks") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("DeadChicks")) != 0)
    {
        deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"));
    }

    // --- Equation ---
    decimal totalChicks = cullsChicks + deadChicks;

    // --- Assign result to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = totalChicks.ToString("0.##");   // format with 2 decimals
}


private void xrTableCell166_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal cullsChicks = 0;
decimal deadChicks = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CullsChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("CullsChicks_1")) != 0)
{
    cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks_1"));
}

if (GetCurrentColumnValue("DeadChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("DeadChicks_1")) != 0)
{
    deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks_1"));
}

// --- Equation ---
decimal totalChicks = cullsChicks + deadChicks;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalChicks.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell171_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
 // --- Declare values ---
decimal erSoldAsHatEggs = 0;
decimal esSoldAsHatEggs = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1")) != 0)
{
    erSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1")) != 0)
{
    esSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1"));
}

// --- Equation ---
decimal totalSoldAsHatEggs = erSoldAsHatEggs + esSoldAsHatEggs;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalSoldAsHatEggs.ToString("0.##");   // format with 2 decimals

}

private void xrTableCell173_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsTableEggs_1 = 0;
decimal esSoldAsTableEggs_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1")) != 0)
{
    erSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1")) != 0)
{
    esSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1"));
}

// --- Equation ---
decimal totalSoldAsTableEggs_1 = erSoldAsTableEggs_1 + esSoldAsTableEggs_1;

// --- Assign result to cell ---
XRTableCell cell_1 = sender as XRTableCell;
cell_1.Text = totalSoldAsTableEggs_1.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell175_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values --- 
decimal erSendToOffal_1 = 0;
decimal esSendToOffal_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SendToOffal_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1")) != 0)
{
    erSendToOffal_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1"));
}

if (GetCurrentColumnValue("ES_SendToOffal_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1")) != 0)
{
    esSendToOffal_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1"));
}

// --- Equation ---
decimal totalSendToOffal_1 = erSendToOffal_1 + esSendToOffal_1;

// --- Assign result to cell ---
XRTableCell cell_1 = sender as XRTableCell;
cell_1.Text = totalSendToOffal_1.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell177_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsHatEggs_1 = 0;
decimal esSoldAsHatEggs_1 = 0;
decimal erSoldAsTableEggs_1 = 0;
decimal esSoldAsTableEggs_1 = 0;
decimal erSendToOffal_1 = 0;
decimal esSendToOffal_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1")) != 0)
{
    erSoldAsHatEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1")) != 0)
{
    esSoldAsHatEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1"));
}

if (GetCurrentColumnValue("ER_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1")) != 0)
{
    erSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1")) != 0)
{
    esSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1"));
}

if (GetCurrentColumnValue("ER_SendToOffal_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1")) != 0)
{
    erSendToOffal_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1"));
}

if (GetCurrentColumnValue("ES_SendToOffal_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1")) != 0)
{
    esSendToOffal_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1"));
}

// --- Equation ---
decimal total_1 = (erSoldAsHatEggs_1 + esSoldAsHatEggs_1) +
                  (erSoldAsTableEggs_1 + esSoldAsTableEggs_1) +
                  (erSendToOffal_1 + esSendToOffal_1);

// --- Assign result to cell ---
XRTableCell cell_1 = sender as XRTableCell;
cell_1.Text = total_1.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell179_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal selChicks_1 = 0;
decimal cullsChicks_1 = 0;
decimal deadChicks_1 = 0;
decimal settedEggs_1 = 0;

// --- Read values directly ---
if (GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.SelChicks_1") != null)
    selChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.SelChicks_1"));

if (GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.CullsChicks_1") != null)
    cullsChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.CullsChicks_1"));

if (GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.DeadChicks_1") != null)
    deadChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.DeadChicks_1"));

if (GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.SettedEggs_1") != null)
    settedEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("SR_HT_FlkSumProdReport_1.SettedEggs_1"));

// --- Calculate CumHatchability ---
decimal cumHatchability_1 = 0;
if (settedEggs_1 != 0)
    cumHatchability_1 = (selChicks_1 + cullsChicks_1 + deadChicks_1) / settedEggs_1 * 100m;

// --- Equation with totalEggsSold ---
decimal result_1 = total_1 * cumHatchability_1 / 100m;

// --- Assign result to cell ---
XRTableCell cell_1 = sender as XRTableCell;
cell_1.Text = result_1.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell187_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
      decimal result = 0;

    // ------------------ EstChicks_PerEgg ------------------
    decimal estChicks_PerEgg = 0;
    decimal cumHatchability = 0;

    object valSetted = GetCurrentColumnValue("SettedEggs");
    if (valSetted != null &amp;&amp; decimal.TryParse(valSetted.ToString(), out decimal settedEggs) &amp;&amp; settedEggs != 0)
    {
        // Sel + Culls + Deads
        decimal sel = 0, culls = 0, dead = 0;

        if (decimal.TryParse(GetCurrentColumnValue("SelChicks")?.ToString(), out decimal tempSel)) sel = tempSel;
        if (decimal.TryParse(GetCurrentColumnValue("CullsChicks")?.ToString(), out decimal tempCulls)) culls = tempCulls;
        if (decimal.TryParse(GetCurrentColumnValue("DeadChicks")?.ToString(), out decimal tempDead)) dead = tempDead;

        cumHatchability = (sel + culls + dead) / settedEggs * 100;
    }

    // use global "total" for TotalEggSold
    estChicks_PerEgg = total * cumHatchability / 100;

    // ------------------ ESTChicks_PerStock_HE ------------------
    decimal estChicks_PerStock_HE = 0;

    if (decimal.TryParse(GetCurrentColumnValue("StockHatEggBal")?.ToString(), out decimal stockBal) &amp;&amp; stockBal != 0)
    {
        // Calculate ActHatchBalanceEggs
        decimal actHatchBalanceEggs = 0;
        if (decimal.TryParse(GetCurrentColumnValue("SetEggBalHatEgg")?.ToString(), out decimal setEggBal) &amp;&amp; setEggBal != 0)
        {
            decimal selBal = 0, cullsBal = 0, deadBal = 0;

            if (decimal.TryParse(GetCurrentColumnValue("SelChickBalHatEgg")?.ToString(), out decimal tempSelBal)) selBal = tempSelBal;
            if (decimal.TryParse(GetCurrentColumnValue("CullsBalHatEgg")?.ToString(), out decimal tempCullsBal)) cullsBal = tempCullsBal;
            if (decimal.TryParse(GetCurrentColumnValue("DeadsBalHatEgg")?.ToString(), out decimal tempDeadBal)) deadBal = tempDeadBal;

            actHatchBalanceEggs = (selBal + cullsBal + deadBal) / setEggBal * 100;
        }

        estChicks_PerStock_HE = stockBal * actHatchBalanceEggs / 100;
    }

    // ------------------ CumChicks ------------------
    decimal cumChicks = 0;
    if (decimal.TryParse(GetCurrentColumnValue("CumChicks")?.ToString(), out decimal tempCum))
        cumChicks = tempCum;

    // ------------------ Final Equation ------------------
    result = estChicks_PerEgg + estChicks_PerStock_HE + cumChicks;

    // Assign to cell
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##");

}

private void xrTableCell214_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal gradeA_1_FromPartA = 0;
decimal rejChicks_1_FromPartA = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("GradeA_1_FromPartA") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("GradeA_1_FromPartA").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("GradeA_1_FromPartA")) != 0)
{
    gradeA_1_FromPartA = Convert.ToDecimal(GetCurrentColumnValue("GradeA_1_FromPartA"));
}

if (GetCurrentColumnValue("RejChicks_1_FromPartA") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejChicks_1_FromPartA").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("RejChicks_1_FromPartA")) != 0)
{
    rejChicks_1_FromPartA = Convert.ToDecimal(GetCurrentColumnValue("RejChicks_1_FromPartA"));
}

// --- Equation ---
decimal total_1 = gradeA_1_FromPartA + rejChicks_1_FromPartA;

// --- Assign result to cell ---
XRTableCell cell_1 = sender as XRTableCell;
cell_1.Text = total_1.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell217_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal settedEggs_1_FromPartA = 0;
decimal cumChicks_1_FromPartA = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("SettedEggs_1_FromPartA") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("SettedEggs_1_FromPartA").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("SettedEggs_1_FromPartA")) != 0)
{
    settedEggs_1_FromPartA = Convert.ToDecimal(GetCurrentColumnValue("SettedEggs_1_FromPartA"));
}

if (GetCurrentColumnValue("CumChicks_1_FromPartA") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumChicks_1_FromPartA").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("CumChicks_1_FromPartA")) != 0)
{
    cumChicks_1_FromPartA = Convert.ToDecimal(GetCurrentColumnValue("CumChicks_1_FromPartA"));
}

// --- Equation ---
decimal result = 0;
if (settedEggs_1_FromPartA != 0)
    result = cumChicks_1_FromPartA * 100 / settedEggs_1_FromPartA;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell220_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal cullsChicks_1 = 0;
decimal deadChicks_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CullsChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CullsChicks_1").ToString()))
{
    cullsChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks_1"));
}

if (GetCurrentColumnValue("DeadChicks_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DeadChicks_1").ToString()))
{
    deadChicks_1 = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks_1"));
}

// --- Equation ---
decimal result = cullsChicks_1 + deadChicks_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");   // format with up to 2 decimals

}

private void xrTableCell226_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsHatEggs_1 = 0;
decimal esSoldAsHatEggs_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1")) != 0)
{
    erSoldAsHatEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsHatEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1")) != 0)
{
    esSoldAsHatEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1"));
}

// --- Equation ---
decimal result = erSoldAsHatEggs_1 + esSoldAsHatEggs_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");  // format with up to 2 decimals

}

private void xrTableCell229_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal erSoldAsTableEggs_1 = 0;
decimal esSoldAsTableEggs_1 = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("ER_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1")) != 0)
{
    erSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1"));
}

if (GetCurrentColumnValue("ES_SoldAsTableEggs_1") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs_1").ToString()) &amp;&amp;
    Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1")) != 0)
{
    esSoldAsTableEggs_1 = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1"));
}

// --- Equation ---
decimal result = erSoldAsTableEggs_1 + esSoldAsTableEggs_1;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("0.##");  // format with up to 2 decimals

}

private void xrTableCell232_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    decimal erValue = 0;
    decimal esValue = 0;

    // --- Read ER value safely ---
    if (GetCurrentColumnValue("ER_SendToOffal_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1")) != 0)
    {
        erValue = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1"));
    }

    // --- Read ES value safely ---
    if (GetCurrentColumnValue("ES_SendToOffal_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1")) != 0)
    {
        esValue = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1"));
    }

    // --- Equation ---
    decimal totalValue = erValue + esValue;

    // --- Assign result to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = totalValue.ToString("0.##"); // display up to 2 decimals
}

private void xrTableCell235_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- Declare variables ---
    decimal erSoldAsHatEggs = 0;
    decimal esSoldAsHatEggs = 0;
    decimal erSoldAsTableEggs = 0;
    decimal esSoldAsTableEggs = 0;
    decimal erSendToOffal = 0;
    decimal esSendToOffal = 0;

    // --- Read ER_SoldAsHatEggs_1 ---
    if (GetCurrentColumnValue("ER_SoldAsHatEggs_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsHatEggs_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1")) != 0)
    {
        erSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsHatEggs_1"));
    }

    // --- Read ES_SoldAsHatEggs_1 ---
    if (GetCurrentColumnValue("ES_SoldAsHatEggs_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsHatEggs_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1")) != 0)
    {
        esSoldAsHatEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsHatEggs_1"));
    }

    // --- Read ER_SoldAsTableEggs_1 ---
    if (GetCurrentColumnValue("ER_SoldAsTableEggs_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SoldAsTableEggs_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1")) != 0)
    {
        erSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ER_SoldAsTableEggs_1"));
    }

    // --- Read ES_SoldAsTableEggs_1 ---
    if (GetCurrentColumnValue("ES_SoldAsTableEggs_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SoldAsTableEggs_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1")) != 0)
    {
        esSoldAsTableEggs = Convert.ToDecimal(GetCurrentColumnValue("ES_SoldAsTableEggs_1"));
    }

    // --- Read ER_SendToOffal_1 ---
    if (GetCurrentColumnValue("ER_SendToOffal_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ER_SendToOffal_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1")) != 0)
    {
        erSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ER_SendToOffal_1"));
    }

    // --- Read ES_SendToOffal_1 ---
    if (GetCurrentColumnValue("ES_SendToOffal_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("ES_SendToOffal_1").ToString()) &amp;&amp;
        Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1")) != 0)
    {
        esSendToOffal = Convert.ToDecimal(GetCurrentColumnValue("ES_SendToOffal_1"));
    }

    // --- Calculate total ---
    decimal total = erSoldAsHatEggs + esSoldAsHatEggs +
                    erSoldAsTableEggs + esSoldAsTableEggs +
                    erSendToOffal + esSendToOffal;

    // --- Assign result to XRTableCell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = total.ToString("0.##");  // formatted to 2 decimals
}

private void xrTableCell189_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
    decimal farmInTake_1 = 0;
    decimal pol = 0;

    // --- Read values safely ---
    if (GetCurrentColumnValue("FarmInTake_1") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("FarmInTake_1").ToString()))
    {
        farmInTake_1 = Convert.ToDecimal(GetCurrentColumnValue("FarmInTake_1"));
    }

    if (GetCurrentColumnValue("POL") != null &amp;&amp;
        !string.IsNullOrEmpty(GetCurrentColumnValue("POL").ToString()))
    {
        pol = Convert.ToDecimal(GetCurrentColumnValue("POL"));
    }

    // --- Equation ---
    decimal result = 0;
    if (pol != 0)
        result = farmInTake_1 / pol;

    // --- Assign result to cell ---
    XRTableCell cell = sender as XRTableCell;
    cell.Text = result.ToString("0.##"); // format with up to 2 decimals
}





private void xrTableCell58_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
       // --- Declare values ---
decimal selChicks = 0;
decimal cullsChicks = 0;
decimal deadChicks = 0;
decimal settedEggs = 0;

// --- Read values directly ---
if (GetCurrentColumnValue("SelChicks") != null)
    selChicks = Convert.ToDecimal(GetCurrentColumnValue("SelChicks"));

if (GetCurrentColumnValue("CullsChicks") != null)
    cullsChicks = Convert.ToDecimal(GetCurrentColumnValue("CullsChicks"));

if (GetCurrentColumnValue("DeadChicks") != null)
    deadChicks = Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"));

if (GetCurrentColumnValue("SettedEggs") != null)
    settedEggs = Convert.ToDecimal(GetCurrentColumnValue("SettedEggs"));

// --- Calculate CumHatchability ---
if (settedEggs != 0)
    cumHatchability = (selChicks + cullsChicks + deadChicks) / settedEggs * 100m;
    
// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = cumHatchability.ToString("0.##"); // format with 2 decimals
}
</value>
  </data>
</root>