<?xml version="1.0" encoding="utf-8"?>
<root>
  <!-- 
    Microsoft ResX Schema 
    
    Version 2.0
    
    The primary goals of this format is to allow a simple XML format 
    that is mostly human readable. The generation and parsing of the 
    various data types are done through the TypeConverter classes 
    associated with the data types.
    
    Example:
    
    ... ado.net/XML headers & schema ...
    <resheader name="resmimetype">text/microsoft-resx</resheader>
    <resheader name="version">2.0</resheader>
    <resheader name="reader">System.Resources.ResXResourceReader, System.Windows.Forms, ...</resheader>
    <resheader name="writer">System.Resources.ResXResourceWriter, System.Windows.Forms, ...</resheader>
    <data name="Name1"><value>this is my long string</value><comment>this is a comment</comment></data>
    <data name="Color1" type="System.Drawing.Color, System.Drawing">Blue</data>
    <data name="Bitmap1" mimetype="application/x-microsoft.net.object.binary.base64">
        <value>[base64 mime encoded serialized .NET Framework object]</value>
    </data>
    <data name="Icon1" type="System.Drawing.Icon, System.Drawing" mimetype="application/x-microsoft.net.object.bytearray.base64">
        <value>[base64 mime encoded string representing a byte array form of the .NET Framework object]</value>
        <comment>This is a comment</comment>
    </data>
                
    There are any number of "resheader" rows that contain simple 
    name/value pairs.
    
    Each data row contains a name, and value. The row also contains a 
    type or mimetype. Type corresponds to a .NET class that support 
    text/value conversion through the TypeConverter architecture. 
    Classes that don't support this are serialized and stored with the 
    mimetype set.
    
    The mimetype is used for serialized objects, and tells the 
    ResXResourceReader how to depersist the object. This is currently not 
    extensible. For a given mimetype the value must be set accordingly:
    
    Note - application/x-microsoft.net.object.binary.base64 is the format 
    that the ResXResourceWriter will generate, however the reader can 
    read any of the formats listed below.
    
    mimetype: application/x-microsoft.net.object.binary.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Binary.BinaryFormatter
            : and then encoded with base64 encoding.
    
    mimetype: application/x-microsoft.net.object.soap.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Soap.SoapFormatter
            : and then encoded with base64 encoding.

    mimetype: application/x-microsoft.net.object.bytearray.base64
    value   : The object must be serialized into a byte array 
            : using a System.ComponentModel.TypeConverter
            : and then encoded with base64 encoding.
    -->
  <xsd:schema id="root" xmlns="" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata">
    <xsd:import namespace="http://www.w3.org/XML/1998/namespace" />
    <xsd:element name="root" msdata:IsDataSet="true">
      <xsd:complexType>
        <xsd:choice maxOccurs="unbounded">
          <xsd:element name="metadata">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" />
              </xsd:sequence>
              <xsd:attribute name="name" use="required" type="xsd:string" />
              <xsd:attribute name="type" type="xsd:string" />
              <xsd:attribute name="mimetype" type="xsd:string" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="assembly">
            <xsd:complexType>
              <xsd:attribute name="alias" type="xsd:string" />
              <xsd:attribute name="name" type="xsd:string" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="data">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
                <xsd:element name="comment" type="xsd:string" minOccurs="0" msdata:Ordinal="2" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" msdata:Ordinal="1" />
              <xsd:attribute name="type" type="xsd:string" msdata:Ordinal="3" />
              <xsd:attribute name="mimetype" type="xsd:string" msdata:Ordinal="4" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="resheader">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" />
            </xsd:complexType>
          </xsd:element>
        </xsd:choice>
      </xsd:complexType>
    </xsd:element>
  </xsd:schema>
  <resheader name="resmimetype">
    <value>text/microsoft-resx</value>
  </resheader>
  <resheader name="version">
    <value>2.0</value>
  </resheader>
  <resheader name="reader">
    <value>System.Resources.ResXResourceReader, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <resheader name="writer">
    <value>System.Resources.ResXResourceWriter, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <data name="ActHatch_Per_Cum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.SettedEggs] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.SettedEggs] * 100, 
    0)
</value>
  </data>
  <data name="ActChicksHHCum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.POL] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.POL], 
    0)
</value>
  </data>
  <data name="dsFinance.ResultSchemaSerializable" xml:space="preserve">
    <value></value>
  </data>
  <data name="CumHatch.Expression" xml:space="preserve">
    <value>Iif(
    [SR_HT_FlockSummary.CumSettingEggs] &gt; 0,
    ([SR_HT_FlockSummary.CumDeadChicks] + [SR_HT_FlockSummary.CumCulls] + [SR_HT_FlockSummary.CumBroiler]) * 100 / [SR_HT_FlockSummary.CumSettingEggs],
    0
)
</value>
  </data>
  <data name="$this.ScriptsSource" xml:space="preserve">
    <value>decimal rejectChicksSum = 0;
decimal selChicksSum  = 0;
decimal eggsSettedSum  = 0 ;
decimal totalCumChicksPercentReport = 0;
decimal totalCumChicksMinusHatchabilityReport = 0;
decimal totalChicksSum  =0;
decimal totalCumBroilerPercentReport  = 0;
decimal totalCumBroilerMinusHatchabilityAGradeReport  = 0;
decimal TotalCullsReport = 0;
decimal TotalDeadChicksReport  = 0;
decimal RejEggsSum = 0;
decimal eggsSettedSumStdbrds = 0;
decimal brdStdAsNumberTotal = 0;
decimal BrdSTDTotal  =0;
decimal totalHatchPrec = 0;
decimal stadHatchabilityDev = 0;
decimal brdStdAsNumberTotalDev = 0;
decimal gtTotalChicksPer = 0;
decimal gtBrdStdAsNumberPercent = 0;
decimal TotalHatch  = 0;
private void Detail_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
       //-- new logic for BrdStd Percentage using total EggsSetted
// --- Get Values ---
decimal eggsSettedSTD = GetCurrentColumnValue("EggsSetted") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
    : 0;

decimal stadHatchability = GetCurrentColumnValue("StadHatchability") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("StadHatchability"))
    : 0;

// --- Calculate @BrdStdAsNumber ---
eggsSettedSumStdbrds += eggsSettedSTD ;
brdStdAsNumberTotal += (stadHatchability * eggsSettedSTD / 100);

//new logic for Dev 
   // --- Calculate @TotalChicks ---
decimal selChicks = GetCurrentColumnValue("SelChicks") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
    : 0;

decimal culls = GetCurrentColumnValue("Culls") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("Culls"))
    : 0;

decimal deadChicks = GetCurrentColumnValue("DeadChicks") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
    : 0;

decimal totalChicks = selChicks + culls + deadChicks;

// --- Calculate @TotalHatchPrec ---
decimal eggsSettedDev = GetCurrentColumnValue("EggsSetted") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
    : 0;

if (eggsSettedDev &gt; 0)
{
    totalHatchPrec = (totalChicks / eggsSettedDev ) * 100;;
}

// --- Get StadHatchability ---
stadHatchabilityDev = GetCurrentColumnValue("StadHatchability") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("StadHatchability"))
    : 0;

//Logic To get Dev for all records 
    // --- Cumulative variables (declare at class level) ---
decimal eggsSettedSumDev = 0;
decimal totalChicksSumDev = 0;

// --- In your script ---
// --- Get current row values ---
decimal eggsSettedHatDev = GetCurrentColumnValue("EggsSetted") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
    : 0;

decimal selChicksDev = GetCurrentColumnValue("SelChicks") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
    : 0;

decimal cullsDev = GetCurrentColumnValue("Culls") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("Culls"))
    : 0;

decimal deadChicksDev = GetCurrentColumnValue("DeadChicks") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
    : 0;

decimal stadHatchabilityHatDev = GetCurrentColumnValue("StadHatchability") != null
    ? Convert.ToDecimal(GetCurrentColumnValue("StadHatchability"))
    : 0;

// --- Calculate current row values ---
decimal currentTotalChicks = selChicksDev + cullsDev + deadChicksDev ;
decimal currentBrdStdAsNumber = stadHatchabilityHatDev * eggsSettedHatDev / 100;

// --- Accumulate sums ---
eggsSettedSumDev += eggsSettedHatDev ;
totalChicksSumDev += currentTotalChicks;
brdStdAsNumberTotalDev += currentBrdStdAsNumber;

// --- Calculate @GT_TotalChicksPer ---
// --- Calculate @GT_TotalChicksPer ---
if (eggsSettedSumDev &gt; 0)
{
    gtTotalChicksPer = (totalChicksSumDev / eggsSettedSumDev) * 100;
}

// --- Calculate @GT_BrdStdAsNumber as percentage ---
if (eggsSettedSumDev &gt; 0)
{
    gtBrdStdAsNumberPercent = (brdStdAsNumberTotalDev / eggsSettedSumDev) * 100;
}
}

private void xrTableCell26_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- Get Values ---
    decimal eggsSetted = GetCurrentColumnValue("EggsSetted") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
        : 0;
    decimal selChicks = GetCurrentColumnValue("SelChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
        : 0;
    // --- Calculate Result ---
    decimal result = (eggsSetted &gt; 0)
        ? (selChicks * 100 / eggsSetted)
        : 0;

    // --- Assign to Cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");
     eggsSettedSum += eggsSetted;
     selChicksSum += selChicks ;

}

private void xrTableCell53_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // --- Use global variables directly ---
    decimal result = (eggsSettedSum &gt; 0)
        ? (selChicksSum * 100 / eggsSettedSum)
        : 0;

    // --- Assign to Cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");
}



private void tdCredit_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
        // --- Get values from the current row ---
    decimal eggsSetted = GetCurrentColumnValue("EggsSetted") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
        : 0;

    decimal selChicks = GetCurrentColumnValue("SelChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("SelChicks"))
        : 0;

    decimal culls = GetCurrentColumnValue("Culls") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("Culls"))
        : 0;

    decimal deadChicks = GetCurrentColumnValue("DeadChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
        : 0;

    // --- TotalChicks = SelChicks + Culls + DeadChicks ---
    decimal totalChicks = selChicks + culls + deadChicks;
    totalChicksSum += totalChicks ;
    // --- Calculate percentage ---
    decimal result = (eggsSetted &gt; 0)
        ? (totalChicks * 100 / eggsSetted)
        : 0;

    // --- Assign to cell ---
   
    ((XRTableCell)sender).Text = result.ToString("0.00");

}

private void xrTableCell57_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Use global variables directly ---
    TotalHatch  = (eggsSettedSum &gt; 0)
        ? (totalChicksSum* 100 / eggsSettedSum)
        : 0;

    // --- Assign to Cell ---
    ((XRTableCell)sender).Text = TotalHatch.ToString("0.00");
}

private void xrTableCell58_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    
// --- Calculate Result ---
if(eggsSettedSumStdbrds &gt; 0){
        
          BrdSTDTotal =  (brdStdAsNumberTotal * 100 / eggsSettedSumStdbrds );
          
                          }

((XRTableCell)sender).Text = BrdSTDTotal.ToString("0.00");


}

private void xrTableCell17_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    //-- new logic for Cum Chicks Percentage ((CumDeadChicks + CumCulls + CumBroiler) * 100 / CumSettingEggs)
// --- Declare variables ---
decimal cumSettingEggs = 0;
decimal cumDeadChicks = 0;
decimal cumCulls = 0;
decimal cumBroiler = 0;
decimal cumChicksPercent = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CumSettingEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumSettingEggs").ToString()))
{
    cumSettingEggs = Convert.ToDecimal(GetCurrentColumnValue("CumSettingEggs"));
}

if (GetCurrentColumnValue("CumDeadChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumDeadChicks").ToString()))
{
    cumDeadChicks = Convert.ToDecimal(GetCurrentColumnValue("CumDeadChicks"));
}

if (GetCurrentColumnValue("CumCulls") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumCulls").ToString()))
{
    cumCulls = Convert.ToDecimal(GetCurrentColumnValue("CumCulls"));
}

if (GetCurrentColumnValue("CumBroiler") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumBroiler").ToString()))
{
    cumBroiler = Convert.ToDecimal(GetCurrentColumnValue("CumBroiler"));
}

// --- Equation (same as Iif(CumSettingEggs &gt; 0, (CumDeadChicks + CumCulls + CumBroiler) * 100 / CumSettingEggs, 0)) ---
if (cumSettingEggs &gt; 0)
{
    cumChicksPercent = (cumDeadChicks + cumCulls + cumBroiler) * 100 / cumSettingEggs;
}
else
{
    cumChicksPercent = 0;
}

// --- Optional: accumulate total ---
totalCumChicksPercentReport += cumChicksPercent;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = cumChicksPercent.ToString("N2");

}

private void xrTableCell41_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell41.Text = totalCumChicksPercentReport.ToString("0");
      totalCumChicksPercentReport= 0; // reset for next group

}

private void xrTableCell28_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    //-- new logic for Cum Chicks Percentage minus StadCumHatchability
// --- Declare variables ---
decimal cumSettingEggs = 0;
decimal cumDeadChicks = 0;
decimal cumCulls = 0;
decimal cumBroiler = 0;
decimal stadCumHatchability = 0;
decimal resultValue = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CumSettingEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumSettingEggs").ToString()))
{
    cumSettingEggs = Convert.ToDecimal(GetCurrentColumnValue("CumSettingEggs"));
}

if (GetCurrentColumnValue("CumDeadChicks") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumDeadChicks").ToString()))
{
    cumDeadChicks = Convert.ToDecimal(GetCurrentColumnValue("CumDeadChicks"));
}

if (GetCurrentColumnValue("CumCulls") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumCulls").ToString()))
{
    cumCulls = Convert.ToDecimal(GetCurrentColumnValue("CumCulls"));
}

if (GetCurrentColumnValue("CumBroiler") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumBroiler").ToString()))
{
    cumBroiler = Convert.ToDecimal(GetCurrentColumnValue("CumBroiler"));
}

if (GetCurrentColumnValue("StadCumHatchability") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("StadCumHatchability").ToString()))
{
    stadCumHatchability = Convert.ToDecimal(GetCurrentColumnValue("StadCumHatchability"));
}

// --- Calculate (CumDeadChicks + CumCulls + CumBroiler) * 100 / CumSettingEggs ---
decimal cumChicksPercent = 0;
if (cumSettingEggs &gt; 0)
{
    cumChicksPercent = (cumDeadChicks + cumCulls + cumBroiler) * 100 / cumSettingEggs;
}

// --- Final result: subtract StadCumHatchability ---
resultValue = cumChicksPercent - stadCumHatchability;

// --- Optional: accumulate total ---
totalCumChicksMinusHatchabilityReport += resultValue;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = resultValue.ToString("N2");

}

private void xrTableCell20_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    //-- new logic for Cum Broiler Percentage (CumBroiler * 100 / CumSettingEggs)
// --- Declare variables ---
decimal cumSettingEggs = 0;
decimal cumBroiler = 0;
decimal cumBroilerPercent = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CumSettingEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumSettingEggs").ToString()))
{
    cumSettingEggs = Convert.ToDecimal(GetCurrentColumnValue("CumSettingEggs"));
}

if (GetCurrentColumnValue("CumBroiler") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumBroiler").ToString()))
{
    cumBroiler = Convert.ToDecimal(GetCurrentColumnValue("CumBroiler"));
}

// --- Equation (same as Iif(CumSettingEggs &gt; 0, CumBroiler * 100 / CumSettingEggs, 0)) ---
if (cumSettingEggs &gt; 0)
{
    cumBroilerPercent = cumBroiler * 100 / cumSettingEggs;
}
else
{
    cumBroilerPercent = 0;
}

// --- Optional: accumulate total ---
totalCumBroilerPercentReport += cumBroilerPercent;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = cumBroilerPercent.ToString("N2");

}

private void xrTableCell22_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    //-- new logic for Cum Broiler Percentage minus StadCumHatchabilityAGrade
// --- Declare variables ---
decimal cumSettingEggs = 0;
decimal cumBroiler = 0;
decimal stadCumHatchabilityAGrade = 0;
decimal resultValue = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("CumSettingEggs") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumSettingEggs").ToString()))
{
    cumSettingEggs = Convert.ToDecimal(GetCurrentColumnValue("CumSettingEggs"));
}

if (GetCurrentColumnValue("CumBroiler") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CumBroiler").ToString()))
{
    cumBroiler = Convert.ToDecimal(GetCurrentColumnValue("CumBroiler"));
}

if (GetCurrentColumnValue("StadCumHatchabilityAGrade") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("StadCumHatchabilityAGrade").ToString()))
{
    stadCumHatchabilityAGrade = Convert.ToDecimal(GetCurrentColumnValue("StadCumHatchabilityAGrade"));
}

// --- Equation (same as (Iif(CumSettingEggs &gt; 0, CumBroiler / CumSettingEggs * 100, 0)) - StadCumHatchabilityAGrade) ---
decimal cumBroilerPercent = 0;

if (cumSettingEggs &gt; 0)
{
    cumBroilerPercent = (cumBroiler / cumSettingEggs) * 100;
}

resultValue = cumBroilerPercent - stadCumHatchabilityAGrade;

// --- Optional: accumulate total ---
totalCumBroilerMinusHatchabilityAGradeReport += resultValue;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = resultValue.ToString("N2");

}



private void xrTableCell6_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     decimal eggsSetted;
    decimal culls;
    decimal deadChicks;
    decimal rejectChicks = 0;
    decimal result = 0;
    
    // Assignments
    eggsSetted = GetCurrentColumnValue("EggsSetted") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("EggsSetted"))
        : 0;
    
    culls = GetCurrentColumnValue("Culls") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("Culls"))
        : 0;
    
    deadChicks = GetCurrentColumnValue("DeadChicks") != null
        ? Convert.ToDecimal(GetCurrentColumnValue("DeadChicks"))
        : 0;
    
    // --- Calculate percentage ---
    rejectChicks = culls + deadChicks;
    
    if (eggsSetted &gt; 0)
    {
        result = (rejectChicks * 100m) / eggsSetted;
    }
         // --- Assign to cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");
    RejEggsSum += eggsSetted ;
    TotalCullsReport += culls ;
    TotalDeadChicksReport += deadChicks ;
    rejectChicksSum = TotalCullsReport  + TotalDeadChicksReport ;

}

private void xrTableCell55_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     // --- Use global variables directly ---
    decimal result = 0 ;
     if(RejEggsSum &gt; 0){
         
           result = (rejectChicksSum  * 100m) / RejEggsSum ;
         }
        
    // --- Assign to Cell ---
    ((XRTableCell)sender).Text = result.ToString("0.00");

}


private void xrTableCell15_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
 
// --- Calculate Final Result: {@TotalHatchPrec} - {SR_HT_FlockSummary.StadHatchability} ---
    decimal finalResult = totalHatchPrec - stadHatchabilityDev;
    ((XRTableCell)sender).Text = finalResult.ToString("0.00");


}

private void xrTableCell59_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
decimal DevTotal =  Math.Round(TotalHatch  , 2)  -  Math.Round(BrdSTDTotal , 2);


// --- Assign to cell ---
((XRTableCell)sender).Text = DevTotal.ToString("0.00");
}
</value>
  </data>
</root>