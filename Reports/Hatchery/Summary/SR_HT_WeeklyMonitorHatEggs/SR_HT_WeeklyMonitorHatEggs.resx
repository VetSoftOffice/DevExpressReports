<?xml version="1.0" encoding="utf-8"?>
<root>
  <!-- 
    Microsoft ResX Schema 
    
    Version 2.0
    
    The primary goals of this format is to allow a simple XML format 
    that is mostly human readable. The generation and parsing of the 
    various data types are done through the TypeConverter classes 
    associated with the data types.
    
    Example:
    
    ... ado.net/XML headers & schema ...
    <resheader name="resmimetype">text/microsoft-resx</resheader>
    <resheader name="version">2.0</resheader>
    <resheader name="reader">System.Resources.ResXResourceReader, System.Windows.Forms, ...</resheader>
    <resheader name="writer">System.Resources.ResXResourceWriter, System.Windows.Forms, ...</resheader>
    <data name="Name1"><value>this is my long string</value><comment>this is a comment</comment></data>
    <data name="Color1" type="System.Drawing.Color, System.Drawing">Blue</data>
    <data name="Bitmap1" mimetype="application/x-microsoft.net.object.binary.base64">
        <value>[base64 mime encoded serialized .NET Framework object]</value>
    </data>
    <data name="Icon1" type="System.Drawing.Icon, System.Drawing" mimetype="application/x-microsoft.net.object.bytearray.base64">
        <value>[base64 mime encoded string representing a byte array form of the .NET Framework object]</value>
        <comment>This is a comment</comment>
    </data>
                
    There are any number of "resheader" rows that contain simple 
    name/value pairs.
    
    Each data row contains a name, and value. The row also contains a 
    type or mimetype. Type corresponds to a .NET class that support 
    text/value conversion through the TypeConverter architecture. 
    Classes that don't support this are serialized and stored with the 
    mimetype set.
    
    The mimetype is used for serialized objects, and tells the 
    ResXResourceReader how to depersist the object. This is currently not 
    extensible. For a given mimetype the value must be set accordingly:
    
    Note - application/x-microsoft.net.object.binary.base64 is the format 
    that the ResXResourceWriter will generate, however the reader can 
    read any of the formats listed below.
    
    mimetype: application/x-microsoft.net.object.binary.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Binary.BinaryFormatter
            : and then encoded with base64 encoding.
    
    mimetype: application/x-microsoft.net.object.soap.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Soap.SoapFormatter
            : and then encoded with base64 encoding.

    mimetype: application/x-microsoft.net.object.bytearray.base64
    value   : The object must be serialized into a byte array 
            : using a System.ComponentModel.TypeConverter
            : and then encoded with base64 encoding.
    -->
  <xsd:schema id="root" xmlns="" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata">
    <xsd:import namespace="http://www.w3.org/XML/1998/namespace" />
    <xsd:element name="root" msdata:IsDataSet="true">
      <xsd:complexType>
        <xsd:choice maxOccurs="unbounded">
          <xsd:element name="metadata">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" />
              </xsd:sequence>
              <xsd:attribute name="name" use="required" type="xsd:string" />
              <xsd:attribute name="type" type="xsd:string" />
              <xsd:attribute name="mimetype" type="xsd:string" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="assembly">
            <xsd:complexType>
              <xsd:attribute name="alias" type="xsd:string" />
              <xsd:attribute name="name" type="xsd:string" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="data">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
                <xsd:element name="comment" type="xsd:string" minOccurs="0" msdata:Ordinal="2" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" msdata:Ordinal="1" />
              <xsd:attribute name="type" type="xsd:string" msdata:Ordinal="3" />
              <xsd:attribute name="mimetype" type="xsd:string" msdata:Ordinal="4" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="resheader">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" />
            </xsd:complexType>
          </xsd:element>
        </xsd:choice>
      </xsd:complexType>
    </xsd:element>
  </xsd:schema>
  <resheader name="resmimetype">
    <value>text/microsoft-resx</value>
  </resheader>
  <resheader name="version">
    <value>2.0</value>
  </resheader>
  <resheader name="reader">
    <value>System.Resources.ResXResourceReader, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <resheader name="writer">
    <value>System.Resources.ResXResourceWriter, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <data name="ActHatch_Per_Cum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.SettedEggs] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.SettedEggs] * 100, 
    0)
</value>
  </data>
  <data name="ActChicksHHCum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.POL] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.POL], 
    0)
</value>
  </data>
  <data name="dsFinance.ResultSchemaSerializable" xml:space="preserve">
    <value></value>
  </data>
  <data name="$this.ScriptsSource" xml:space="preserve">
    <value>decimal  StainedPercReport = 0;
decimal AdheringShavingPercReport  = 0;
decimal avgPercentageDirtyEgg = 0;
decimal avgPercentageAdheringShaving = 0;
decimal avgPercentageStained  = 0;
decimal avgPercentageTotalDirty  = 0;
decimal avgPercentageSlabSides = 0;
decimal avgPercentageBodyCheck = 0;
decimal avgPercentageWrinkled = 0;
decimal avgPercentageThinSpot  =0;
decimal avgPercentageShellDefects = 0;
decimal avgPercentageUpsideDown = 0;
private void tdCostCenter_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
  // --- Declare values ---
decimal stained = 0;
decimal sampleQty = 0;

// --- Read values ---
if (GetCurrentColumnValue("Stained") != null)
    stained = Convert.ToDecimal(GetCurrentColumnValue("Stained"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (stained * 100m) / sampleQty;

// --- Add to report totals (for average) ---
decimal StainedPercReportSum = 0;
int StainedPercReportCount = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
StainedPercReportSum += percentage;   // sum of percentages
StainedPercReportCount++;             // count how many percentages added

// --- Compute running average ---
decimal avgPercentage = 0;
if (StainedPercReportCount &gt; 0)
    avgPercentageStained = StainedPercReportSum / StainedPercReportCount;


}

private void xrTableCell43_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    xrTableCell43.Text = avgPercentageStained.ToString("N2");

}

private void tdDebit_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
// --- Declare values --- 
decimal adheringShaving = 0;
decimal sampleQty = 0;

// --- Read values ---
if (GetCurrentColumnValue("AdheringShaving") != null)
    adheringShaving = Convert.ToDecimal(GetCurrentColumnValue("AdheringShaving"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (adheringShaving * 100m) / sampleQty;

// --- Add to report totals (for average) ---
decimal AdheringShavingPercReportSum  = 0;
int AdheringShavingPercReportCount = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
AdheringShavingPercReportSum += percentage;   // keep sum of percentages
AdheringShavingPercReportCount++;             // count how many percentages added

// --- Compute running average ---
if (AdheringShavingPercReportCount &gt; 0)
    avgPercentageAdheringShaving = AdheringShavingPercReportSum / AdheringShavingPercReportCount;


}

private void xrTableCell45_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell45.Text = avgPercentageAdheringShaving.ToString("N2");

}

private void xrTableCell56_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
// --- Declare values ---  
decimal dirtyEgg = 0;
decimal sampleQty = 0;
decimal DirtyEggPercReportCount = 0;
// --- Read values ---
if (GetCurrentColumnValue("DirtyEgg") != null)
    dirtyEgg = Convert.ToDecimal(GetCurrentColumnValue("DirtyEgg"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (dirtyEgg * 100m) / sampleQty;

// --- Add to report totals (for average) ---
decimal DirtyEggPercReportSum  = 0;
int DirtyEggPercReportCount = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
DirtyEggPercReportSum += percentage;   // keep sum of percentages
DirtyEggPercReportCount++;             // count how many percentages added

// --- Compute running average ---
decimal avgPercentage = 0;
if (DirtyEggPercReportCount &gt; 0)
    avgPercentageDirtyEgg = DirtyEggPercReportSum / DirtyEggPercReportCount;


}

private void xrTableCell47_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell47.Text = avgPercentageDirtyEgg.ToString("0");

}

private void tdCredit_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
       // --- Declare values ---
decimal stained = 0;
decimal adheringShaving = 0;
decimal dirtyEgg = 0;
decimal sampleQty = 0;

// --- Read values ---
if (GetCurrentColumnValue("Stained") != null)
    stained = Convert.ToDecimal(GetCurrentColumnValue("Stained"));

if (GetCurrentColumnValue("AdheringShaving") != null)
    adheringShaving = Convert.ToDecimal(GetCurrentColumnValue("AdheringShaving"));

if (GetCurrentColumnValue("DirtyEgg") != null)
    dirtyEgg = Convert.ToDecimal(GetCurrentColumnValue("DirtyEgg"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = ((stained + adheringShaving + dirtyEgg) * 100m) / sampleQty;

// --- Add to report totals (for average) ---
decimal TotalSpecialEggsPercSum  = 0;
int TotalSpecialEggsPercCount = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
TotalSpecialEggsPercSum += percentage;   // sum of combined percentages
TotalSpecialEggsPercCount++;             // count of rows

// --- Compute running average ---
if (TotalSpecialEggsPercCount &gt; 0)
    avgPercentageTotalDirty = TotalSpecialEggsPercSum / TotalSpecialEggsPercCount;


}

private void xrTableCell49_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell49.Text = avgPercentageTotalDirty.ToString("0");

}

private void xrTableCell15_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---  
decimal slabSides = 0;
decimal sampleQty = 0;

// --- Read values ---  
if (GetCurrentColumnValue("SlabSides") != null)
    slabSides = Convert.ToDecimal(GetCurrentColumnValue("SlabSides"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---  
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (slabSides * 100m) / sampleQty;

// --- Add to report totals (for average) ---  
decimal SlabSidesPercReportSum = 0;
int SlabSidesPercReportCount = 0;

// --- Assign result to cell ---  
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
SlabSidesPercReportSum += percentage;   // keep sum of percentages  
SlabSidesPercReportCount++;             // count how many percentages added  

// --- Compute running average ---  
if (SlabSidesPercReportCount &gt; 0)
    avgPercentageSlabSides = SlabSidesPercReportSum / SlabSidesPercReportCount;


}

private void xrTableCell51_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
      xrTableCell51.Text = avgPercentageSlabSides.ToString("N2");


}

private void xrTableCell17_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---  
decimal bodyCheck = 0;
decimal sampleQty = 0;

// --- Read values ---  
if (GetCurrentColumnValue("BodyCheck") != null)
    bodyCheck = Convert.ToDecimal(GetCurrentColumnValue("BodyCheck"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---  
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (bodyCheck * 100m) / sampleQty;

// --- Add to report totals (for average) ---  
decimal BodyCheckPercReportSum = 0;
int BodyCheckPercReportCount = 0;

// --- Assign result to cell ---  
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
BodyCheckPercReportSum += percentage;   // keep sum of percentages  
BodyCheckPercReportCount++;             // count how many percentages added  

// --- Compute running average ---  
if (BodyCheckPercReportCount &gt; 0)
    avgPercentageBodyCheck = BodyCheckPercReportSum / BodyCheckPercReportCount;


}

private void xrTableCell57_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
          xrTableCell57.Text = avgPercentageBodyCheck.ToString("0");

}

private void xrTableCell28_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---  
decimal wrinkled = 0;
decimal sampleQty = 0;

// --- Read values ---  
if (GetCurrentColumnValue("Wrinkled") != null)
    wrinkled = Convert.ToDecimal(GetCurrentColumnValue("Wrinkled"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---  
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (wrinkled * 100m) / sampleQty;

// --- Add to report totals (for average) ---  
decimal WrinkledPercReportSum = 0;
int WrinkledPercReportCount = 0;

// --- Assign result to cell ---  
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
WrinkledPercReportSum += percentage;   // keep sum of percentages  
WrinkledPercReportCount++;             // count how many percentages added  

// --- Compute running average ---  
if (WrinkledPercReportCount &gt; 0)
    avgPercentageWrinkled = WrinkledPercReportSum / WrinkledPercReportCount;


}

private void xrTableCell59_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell59.Text = avgPercentageWrinkled.ToString("N2");

}

private void xrTableCell19_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---  
decimal thinSpot = 0;
decimal sampleQty = 0;

// --- Read values ---  
if (GetCurrentColumnValue("ThinSpot") != null)
    thinSpot = Convert.ToDecimal(GetCurrentColumnValue("ThinSpot"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---  
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (thinSpot * 100m) / sampleQty;

// --- Add to report totals (for average) ---  
decimal ThinSpotPercReportSum = 0;
int ThinSpotPercReportCount = 0;

// --- Assign result to cell ---  
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
ThinSpotPercReportSum += percentage;   // keep sum of percentages  
ThinSpotPercReportCount++;             // count how many percentages added  

// --- Compute running average ---  
decimal avgPercentageThinSpot = 0;
if (ThinSpotPercReportCount &gt; 0)
    avgPercentageThinSpot = ThinSpotPercReportSum / ThinSpotPercReportCount;


}

private void xrTableCell61_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    xrTableCell61.Text = avgPercentageThinSpot.ToString("N2");


}

private void xrTableCell21_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal slabSides = 0;
decimal bodyCheck = 0;
decimal wrinkled = 0;
decimal thinSpot = 0;
decimal sampleQty = 0;

// --- Read values ---
if (GetCurrentColumnValue("SlabSides") != null)
    slabSides = Convert.ToDecimal(GetCurrentColumnValue("SlabSides"));

if (GetCurrentColumnValue("BodyCheck") != null)
    bodyCheck = Convert.ToDecimal(GetCurrentColumnValue("BodyCheck"));

if (GetCurrentColumnValue("Wrinkled") != null)
    wrinkled = Convert.ToDecimal(GetCurrentColumnValue("Wrinkled"));

if (GetCurrentColumnValue("ThinSpot") != null)
    thinSpot = Convert.ToDecimal(GetCurrentColumnValue("ThinSpot"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = ((slabSides + bodyCheck + wrinkled + thinSpot) * 100m) / sampleQty;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
// --- Add to report totals (for average) ---
TotalShellDefectsPercSum += percentage;   // sum of combined percentages
TotalShellDefectsPercCount++;             // count of rows

// --- Compute running average ---
if (TotalShellDefectsPercCount &gt; 0)
    avgPercentageShellDefects = TotalShellDefectsPercSum / TotalShellDefectsPercCount;



}

private void xrTableCell63_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
     xrTableCell63.Text = avgPercentageShellDefects.ToString("N2");

}

private void xrTableCell25_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare values ---
decimal upsideDown = 0;
decimal sampleQty = 0;

// --- Read values ---
if (GetCurrentColumnValue("UpsideDown") != null)
    upsideDown = Convert.ToDecimal(GetCurrentColumnValue("UpsideDown"));

if (GetCurrentColumnValue("SampleQty") != null)
    sampleQty = Convert.ToDecimal(GetCurrentColumnValue("SampleQty"));

// --- Equation ---
decimal percentage = 0;
if (sampleQty &gt; 0)
    percentage = (upsideDown * 100m) / sampleQty;

// --- Add to report totals (for average) ---
decimal UpsideDownPercReportSum = 0;
int UpsideDownPercReportCount = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = percentage.ToString("0.##"); // format with up to 2 decimals
UpsideDownPercReportSum += percentage;   // keep sum of percentages
UpsideDownPercReportCount++;             // count how many percentages added

// --- Compute running average ---
if (UpsideDownPercReportCount &gt; 0)
    avgPercentageUpsideDown = UpsideDownPercReportSum / UpsideDownPercReportCount;


}
private void xrTableCell65_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
       xrTableCell65.Text = avgPercentageUpsideDown.ToString("N2");

}



</value>
  </data>
</root>