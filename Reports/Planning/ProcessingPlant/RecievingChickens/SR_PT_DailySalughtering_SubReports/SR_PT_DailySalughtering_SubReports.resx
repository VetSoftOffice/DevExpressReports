<?xml version="1.0" encoding="utf-8"?>
<root>
  <!-- 
    Microsoft ResX Schema 
    
    Version 2.0
    
    The primary goals of this format is to allow a simple XML format 
    that is mostly human readable. The generation and parsing of the 
    various data types are done through the TypeConverter classes 
    associated with the data types.
    
    Example:
    
    ... ado.net/XML headers & schema ...
    <resheader name="resmimetype">text/microsoft-resx</resheader>
    <resheader name="version">2.0</resheader>
    <resheader name="reader">System.Resources.ResXResourceReader, System.Windows.Forms, ...</resheader>
    <resheader name="writer">System.Resources.ResXResourceWriter, System.Windows.Forms, ...</resheader>
    <data name="Name1"><value>this is my long string</value><comment>this is a comment</comment></data>
    <data name="Color1" type="System.Drawing.Color, System.Drawing">Blue</data>
    <data name="Bitmap1" mimetype="application/x-microsoft.net.object.binary.base64">
        <value>[base64 mime encoded serialized .NET Framework object]</value>
    </data>
    <data name="Icon1" type="System.Drawing.Icon, System.Drawing" mimetype="application/x-microsoft.net.object.bytearray.base64">
        <value>[base64 mime encoded string representing a byte array form of the .NET Framework object]</value>
        <comment>This is a comment</comment>
    </data>
                
    There are any number of "resheader" rows that contain simple 
    name/value pairs.
    
    Each data row contains a name, and value. The row also contains a 
    type or mimetype. Type corresponds to a .NET class that support 
    text/value conversion through the TypeConverter architecture. 
    Classes that don't support this are serialized and stored with the 
    mimetype set.
    
    The mimetype is used for serialized objects, and tells the 
    ResXResourceReader how to depersist the object. This is currently not 
    extensible. For a given mimetype the value must be set accordingly:
    
    Note - application/x-microsoft.net.object.binary.base64 is the format 
    that the ResXResourceWriter will generate, however the reader can 
    read any of the formats listed below.
    
    mimetype: application/x-microsoft.net.object.binary.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Binary.BinaryFormatter
            : and then encoded with base64 encoding.
    
    mimetype: application/x-microsoft.net.object.soap.base64
    value   : The object must be serialized with 
            : System.Runtime.Serialization.Formatters.Soap.SoapFormatter
            : and then encoded with base64 encoding.

    mimetype: application/x-microsoft.net.object.bytearray.base64
    value   : The object must be serialized into a byte array 
            : using a System.ComponentModel.TypeConverter
            : and then encoded with base64 encoding.
    -->
  <xsd:schema id="root" xmlns="" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:msdata="urn:schemas-microsoft-com:xml-msdata">
    <xsd:import namespace="http://www.w3.org/XML/1998/namespace" />
    <xsd:element name="root" msdata:IsDataSet="true">
      <xsd:complexType>
        <xsd:choice maxOccurs="unbounded">
          <xsd:element name="metadata">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" />
              </xsd:sequence>
              <xsd:attribute name="name" use="required" type="xsd:string" />
              <xsd:attribute name="type" type="xsd:string" />
              <xsd:attribute name="mimetype" type="xsd:string" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="assembly">
            <xsd:complexType>
              <xsd:attribute name="alias" type="xsd:string" />
              <xsd:attribute name="name" type="xsd:string" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="data">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
                <xsd:element name="comment" type="xsd:string" minOccurs="0" msdata:Ordinal="2" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" msdata:Ordinal="1" />
              <xsd:attribute name="type" type="xsd:string" msdata:Ordinal="3" />
              <xsd:attribute name="mimetype" type="xsd:string" msdata:Ordinal="4" />
              <xsd:attribute ref="xml:space" />
            </xsd:complexType>
          </xsd:element>
          <xsd:element name="resheader">
            <xsd:complexType>
              <xsd:sequence>
                <xsd:element name="value" type="xsd:string" minOccurs="0" msdata:Ordinal="1" />
              </xsd:sequence>
              <xsd:attribute name="name" type="xsd:string" use="required" />
            </xsd:complexType>
          </xsd:element>
        </xsd:choice>
      </xsd:complexType>
    </xsd:element>
  </xsd:schema>
  <resheader name="resmimetype">
    <value>text/microsoft-resx</value>
  </resheader>
  <resheader name="version">
    <value>2.0</value>
  </resheader>
  <resheader name="reader">
    <value>System.Resources.ResXResourceReader, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <resheader name="writer">
    <value>System.Resources.ResXResourceWriter, System.Windows.Forms, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</value>
  </resheader>
  <data name="ActHatch_Per_Cum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.SettedEggs] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.SettedEggs] * 100, 
    0)
</value>
  </data>
  <data name="ActChicksHHCum.Expression" xml:space="preserve">
    <value>Iif([SR_HT_WeeklyHHFlockSum.POL] &lt;&gt; 0, 
    ([SR_HT_WeeklyHHFlockSum.ActChicks] + [SR_HT_WeeklyHHFlockSum.CullsChicks] + [SR_HT_WeeklyHHFlockSum.DeadChicks]) 
    / [SR_HT_WeeklyHHFlockSum.POL], 
    0)
</value>
  </data>
  <data name="AcceptQty.Expression" xml:space="preserve">
    <value>Iif(IsNull([RecQty]) Or [RecQty] = '', 0, [RecQty]) 
- Iif(IsNull([MortUnderWt]) Or [MortUnderWt] = '', 0, [MortUnderWt]) 
+ Iif(IsNull([MedicalCondemnation]) Or [MedicalCondemnation] = '', 0, [MedicalCondemnation]) 
+ Iif(IsNull([PluckReject]) Or [PluckReject] = '', 0, [PluckReject])
</value>
  </data>
  <data name="AcceptQtyWt.Expression" xml:space="preserve">
    <value>Iif(IsNull([RecWeight]) Or [RecWeight] = '', 0, [RecWeight]) 
- Iif(IsNull([MortUnerWtWt]) Or [MortUnerWtWt] = '', 0, [MortUnerWtWt]) 
+ Iif(IsNull([MedicalCondWeight]) Or [MedicalCondWeight] = '', 0, [MedicalCondWeight]) 
+ Iif(IsNull([PluckRejectWeight]) Or [PluckRejectWeight] = '', 0, [PluckRejectWeight])
</value>
  </data>
  <data name="dsFinance.ResultSchemaSerializable" xml:space="preserve">
    <value></value>
  </data>
  <data name="$this.ScriptsSource" xml:space="preserve">
    <value>decimal recQtySum = 0;
decimal doaSum = 0;
decimal recQtyRejSum = 0;
decimal rejectedQtySum = 0;
decimal recQtyPlukRejSum = 0;
decimal pluckRejectSum = 0;
decimal recQtyMedicalCondemnationSum  = 0;
decimal medicalCondemnationSum  = 0;
decimal recQtyAcceptWtSum = 0;
decimal acceptWtSum = 0;

private void xrTableCell24_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal doa = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("DOA") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("DOA").ToString()))
{
    doa = Convert.ToDecimal(GetCurrentColumnValue("DOA"));
}

// --- Accumulate totals across all records ---

recQtySum += recQty;
doaSum += doa;

// --- Equation (same as Iif(Sum([RecQty]) &lt;&gt; 0, Sum([DOA]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtySum != 0)
    result = (doaSum * 100) / recQtySum;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // format with 2 decimal places
}

private void xrTableCell27_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal rejectedQty = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("RejectedQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RejectedQty").ToString()))
{
    rejectedQty = Convert.ToDecimal(GetCurrentColumnValue("RejectedQty"));
}


recQtyRejSum += recQty;
rejectedQtySum += rejectedQty;

// --- Equation (same as Iif(Sum([RecQty]) &lt;&gt; 0, Sum([RejectedQty]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtyRejSum != 0)
    result = (rejectedQtySum * 100) / recQtyRejSum ;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // format with 2 decimal places

}

private void xrTableCell36_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal pluckReject = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("PluckReject") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("PluckReject").ToString()))
{
    pluckReject = Convert.ToDecimal(GetCurrentColumnValue("PluckReject"));
}

// --- Accumulate totals across all records ---

recQtyPlukRejSum += recQty;
pluckRejectSum += pluckReject;

// --- Equation (Iif(Sum([RecQty]) &lt;&gt; 0, Sum([PluckReject]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtyPlukRejSum != 0)
    result = (pluckRejectSum * 100) / recQtyPlukRejSum ;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // format with 2 decimal places

}

private void xrTableCell37_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal medicalCondemnation = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("MedicalCondemnation") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("MedicalCondemnation").ToString()))
{
    medicalCondemnation = Convert.ToDecimal(GetCurrentColumnValue("MedicalCondemnation"));
}

// --- Accumulate totals across all records ---

recQtyMedicalCondemnationSum += recQty;
medicalCondemnationSum += medicalCondemnation;

// --- Equation (Iif(Sum([RecQty]) &lt;&gt; 0, Sum([MedicalCondemnation]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtyMedicalCondemnationSum != 0)
    result = (medicalCondemnationSum * 100) / recQtyMedicalCondemnationSum ;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // format with 2 decimal places

}

private void xrTableCell38_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal acceptWt = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("AcceptWt") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("AcceptWt").ToString()))
{
    acceptWt = Convert.ToDecimal(GetCurrentColumnValue("AcceptWt"));
}

// --- Accumulate totals across all records ---

recQtyAcceptWtSum += recQty;
acceptWtSum += acceptWt;

// --- Equation (Iif(Sum([RecQty]) &lt;&gt; 0, Sum([AcceptWt]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtyAcceptWtSum != 0)
    result = (acceptWtSum * 100) / recQtyAcceptWtSum ;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // Format with 2 decimal places

}

private void xrTableCell39_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variables for current record ---
decimal recQty = 0;
decimal counterQty = 0;

// --- Read values safely ---
if (GetCurrentColumnValue("RecQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("RecQty").ToString()))
{
    recQty = Convert.ToDecimal(GetCurrentColumnValue("RecQty"));
}

if (GetCurrentColumnValue("CounterQty") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("CounterQty").ToString()))
{
    counterQty = Convert.ToDecimal(GetCurrentColumnValue("CounterQty"));
}

// --- Accumulate totals across all records ---
recQtyCounterSum += recQty;
counterQtySum += counterQty;

// --- Equation (Iif(Sum([RecQty]) &lt;&gt; 0, Sum([CounterQty]) * 100 / Sum([RecQty]), 0)) ---
decimal result = 0;
if (recQtyCounterSum != 0)
    result = (counterQtySum * 100) / recQtyCounterSum ;
else
    result = 0;

// --- Assign result to cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = result.ToString("N2"); // format with 2 decimal places

}

private void xrTableCell69_BeforePrint(object sender, System.ComponentModel.CancelEventArgs e) {
    // --- Declare local variable for current record ---
decimal totalProblem = 0;

// --- Read value safely ---
if (GetCurrentColumnValue("TotalProblem") != null &amp;&amp;
    !string.IsNullOrEmpty(GetCurrentColumnValue("TotalProblem").ToString()))
{
    totalProblem = Convert.ToDecimal(GetCurrentColumnValue("TotalProblem"));
}

// --- Accumulate total across all records ---
// (Define this field once at the top of your report class)
// decimal totalProblemSum = 0;

totalProblemSum += totalProblem;

// --- Assign result to the cell ---
XRTableCell cell = sender as XRTableCell;
cell.Text = totalProblemSum.ToString("N2"); // format with 2 decimal places

}
</value>
  </data>
</root>